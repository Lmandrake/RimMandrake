{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "codex_queue_runner job manifest",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Absolute path of the saved image file, in the worker's own working directory."
    },
    "notes": {
      "type": "string",
      "description": "One line: what was drawn, or what was assumed if the prompt was ambiguous."
    }
  },
  "required": ["path", "notes"],
  "additionalProperties": false
}
