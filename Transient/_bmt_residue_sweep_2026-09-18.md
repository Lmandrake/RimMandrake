# BMT_ residue sweep — CAVERNS_PARITY_BUILD_1 gates (c) and (f) — 2026-09-18

Status: IN PROGRESS (skeleton)

## Scope
- (c) src/RimUtinni/UtinniPatches/Patches/BiomeFlora_Ashkarr.xml — repoint BMT_ plant rows
- (f) 7 ruled-cut straggler creatures — delete rows: ChemSnail, CaveSpider, GiantSlug, GiantSnail, Pillbug, GlowBat
- Also check src/RimUtinni/UtinniPatches/Patches/RUT_TheRot.xml (Brightbells flora token + straggler animal rows)

## Evidence gathered
- BiomeFlora_Ashkarr.xml (GENERATED by design/Jawa/mods/biome_flora.py, "do not
  hand-edit" header) carries 104 BMT_ tokens / 52 rows / 39 distinct defNames
  (MEASURED, matches scoping doc). Editing it by hand per explicit task brief;
  generator source not touched (out of write scope) so a future regen from
  biome_flora.py will REVERT these edits unless the generator's own source
  data is updated too — flagged as owed follow-up, not done here.
- RUT_TheRot.xml (BiomeDef, src/RimUtinni/UtinniPatches/Defs/BiomeDefs/) wildPlants
  block already carries the RotSporeKit RUT_ ports of 18 of the 39 tokens, each
  commented "RUT_ (raided)" — this is the authoritative BMT_->RUT_ map for those
  18 (FUNGALFOREST_RAID_MERGE_1). Confirmed via RUT_RotSporeKit_Flora.xml
  (src/RimUtinni/RotSporeKit/Defs/ThingDefs_Plants/) defName list.
- decisions_propagated.json is fauna-only (`fauna:`/`q`/`homeless` keys only,
  zero flora rows) — confirms CLAUDE.md's known fact; not usable for flora.
- caverns_replacement_scoping.md §1/§4: the remaining 21 tokens are Polluted-Lands-
  lineage (BMT_Plant_* naming + Heatsink/FireLavender/RainbowTongue/GiantLeaf) whose
  fate is GATED on an owner ruling not yet made (§5 Q2: drop vs replace the Polluted
  Lands mutation system). No RUT_/RSW_ replacement exists for them yet -> UNMAPPED.
- Gate (f) stragglers: RUT_TheRot.xml wildAnimals has BMT_ChemSnail, BMT_GlowBat,
  BMT_Pillbug, BMT_GiantSlug, BMT_GiantSnail, BMT_CaveSpider (6 rows). RUT_CrackedLands.xml
  wildAnimals has a second BMT_ChemSnail row. BiomeCast_Ashkarr.xml (also GENERATED, by
  design/Jawa/mods/gen_cast_patch.py from cast_assignment.csv) mirrors the same 7 rows at
  the_rot + cracked_lands cast blocks.
- cast_assignment.csv (design/Jawa/fauna/, OUT of my write scope) still records "keep"/
  "import" decisions for all 7 stragglers — contradicts the 2026-09-11 CUT ruling. Left
  untouched; flagged for BENCH, since gen_cast_patch.py will resurrect the cast rows I
  deleted in BiomeCast_Ashkarr.xml the next time it regenerates from that CSV.
- Newest def dump for validate_patch.py --defs: `2026-09-18T17-31-09Z` under
  `RimWorld by Ludeon Studios/DefDump/captures/` (Windows AppData, reached via /mnt/c
  in this WSL session).

## Mapping BMT_ -> replacement
See MAPPING section of final report (18 rows, all sourced from RUT_TheRot.xml /
RUT_RotSporeKit_Flora.xml "RUT_ (raided)" comments).

## Unmapped
21 Polluted-Lands-lineage tokens, gated on caverns_replacement_scoping.md §5 Q2
(owner has not ruled drop vs replace). See UNMAPPED section of final report.

## Edits made
(fill in as executed)

## Validator results
(fill in)

## Open questions / unknowns
- BiomeFlora_Ashkarr.xml / BiomeCast_Ashkarr.xml are both GENERATED files edited
  by hand per explicit task brief; their generators' source data was not updated
  (out of scope), so a future regen reverts today's edits unless someone updates
  biome_flora.py's/gen_cast_patch.py's source (cast_assignment.csv for the latter)
  to match.
- RUT_Nuitae / RUT_Arpeau / RUT_GreyLady are each defined TWICE — once in
  LanternDeeps/RUT_DeepFlora.xml (mandrake.rut.lanterndeeps) and once in
  RotSporeKit/RUT_RotSporeKit_Flora.xml (mandrake.rut.rotsporekit) — a real
  duplicate-defName collision. Not investigated further: LanternDeeps is
  forbidden territory for this task and it's outside gates (c)/(f). Flagged only.
