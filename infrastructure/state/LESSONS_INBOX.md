# Lessons inbox — one line each, curated into skills in fresh-context passes

- 2026-09-07: `skills/generating-images/SKILL.md` still documents a `--chroma-key '#00ff00'` flag on `codex_image.py generate`, but that flag was removed 2026-09-06 (chroma-key retired from the generation path; `chroma_key.py` is now a separate post-process script called directly by callers like `build_sea_facings.py`). SKILL.md needs its workflow section updated to match.
- prove_new_tools census: jawa/revoke reads MISSING at Entry (314/315) — first sighting; suspect tool_surface upper-bound counting a message string, not a real tool (2026-09-07 BENCH)
- 2026-09-08 FOUNDRY: `git commit`/`add` can fail with `.git/index.lock` mid-turn when another window's session is committing concurrently in the same shared worktree — retry once it clears, don't force-remove without confirming abandonment.
- 2026-09-08 FOUNDRY: `python.exe` (WSL calling Windows Python for bridge scripts) can't open a script path under `/tmp/...` or the session scratchpad — write bridge test scripts into `Transient/` and run with a repo-relative path.
- 2026-09-08 FOUNDRY: `rimflow bridge`'s 45-min staleness rule measures event silence, not game activity — before restarting/swapping the mod list on a "stale" bridge hold, cross-check `rimworld/get_game_info` and Player.log's own mtime; a session can be genuinely live despite a long-idle bridge ledger.
- BENCH 2026-09-08: bridge lock heartbeat — driving the bridge does not refresh the lock (only rimflow ledger events do); I drove ~1.5h of reband/crag work emitting none, so my lock read stale. Emit a periodic note while driving. (Not the cause of the 2026-09-08 crash — that was FOUNDRY's cold-load/tilegen ntdll crash, `BRIDGE_NTDLL_CRASH_TILEGEN_1`; my concurrent world_mutators_get ConnectionReset was a symptom of the game dying. V16 held all worldmap work.)
- BENCH 2026-09-08: the canon CSV `biome` column is the PRE-REBAND world; current biome = CSV overlaid with the 8 world/*_plan.json files in order. FungalForest(425)/SeaIce(262)/Lake(10) were "dissolved" in docs+a save yet sat live for days because everyone read the CSV. Measure LIVE via the bridge or reconstruct with the overlays — never the CSV's biome column alone.
- BENCH 2026-09-08: bridge tool param-name traps — jawa/world_landmarks_get takes `limit` not `range`; world_features objects key the id as `uniqueID` not `featureId`, and world_features_set assign/update/delete needs that uniqueID (a wrong name returns None silently).
- BENCH 2026-09-08: deserts/wasteland/seas SHOULD read barren on the worldmap — sparse is correct; only dense/dramatic biomes want a landmark-density pass. Blanket-landmarking everything is a defect.
- FOUNDRY 2026-09-08: `handoff.py`'s `since_ts` came from `git log --format=%cI` (LOCAL offset, e.g. `-07:00`) compared via bare string `>` against ledger `ts` values (`Z`-suffixed UTC) — two different ISO-8601 formats, so `'13:07:06Z' > '06:19:54-07:00'` read True even though the first is chronologically EARLIER. Silently pulled two prior-window item starts into "started this window". Fixed by normalizing to UTC `Z` at the source (`_to_utc_z`); any OTHER code comparing a `%cI` git timestamp against a ledger timestamp has the same exposure.
- FOUNDRY 2026-09-08: a `git add`+`git commit` done as two separate tool calls can lose the race to another window's concurrent commit — my staged files got swept into BENCH's own commit (content survived, intact and pushed, but under BENCH's commit message with no mention of what I'd actually done). Stage and commit as close together as possible, and if a commit reports "no changes added" right after a successful `git add`, check whether your changes already landed in someone else's HEAD before re-adding.
- 2026-09-08 BENCH: /mnt/d drvfs served STALE reads cross-process for ~minutes (git status, grep, tail all showed pre-write content while the writes were durable) — a 'reverted file' on this mount needs a re-read before any recovery action; I nearly re-applied work that was never lost. Also: a peer's 'git reset --hard HEAD' appeared in reflog mid-session — harmless this time, but it is the forbidden destructive act in a shared tree.
- FOUNDRY 2026-09-08: a subagent's clean `dotnet build`/`validate_patch.py` pass and a real commit+push are NOT proof the fix was deployed — twice this wave (`ARMOURY_PATCH_INNER_MISS_1`, `WORLD_LINT_WATER_HARDCODE_1`) a genuinely-fixed patch/DLL sat undeployed in the repo while a live re-test still showed the old failure. Deploying static def/XML output needs no running game — always `deploy_custom_mods.py --mod <X>` (plan-only first) before trusting a live re-check of anything a prior pass claims fixed.
- FOUNDRY 2026-09-08: `rimworld/start_debug_game_ready` crashed the game outright (not just hung) on the owner's full ~599-mod list — mid-worldgen, `Player.log` stopped growing for 5+ min, then the process was gone with no crash dialog. Never run it on the full list; swap to the minimal list first. A normal boot-to-menu or real save load on the full list is fine — only quicktest worldgen on that stack is dangerous.
- FOUNDRY 2026-09-08: a QuestScriptDef with `rootSelectionWeight > 0` may NOT also have an IncidentDef pointing at it — `IncidentDef.ConfigErrors()` tests `questScriptDef.rootSelectionWeight != 0f` and never reads `baseChance`, so shipping the incident at `baseChance 0` as an inert "deterministic test trigger" is still a config error ("quest is run from both incident and random quest"). The two firing routes in the rimworld-quests skill §7 are mutually exclusive, not additive; `validate_quest.py` does not catch it (it only sees ONE def at a time). Force a natural-pool quest with dev Generate-quest or `jawa/fire_quest`, never a companion IncidentDef.
- naming_lint.py counts defNames inside XML comments — 5 false positives survive a clean R4 pass (2026-09-08, MOD_CONSOLIDATION_SPRINT_1); needs comment-stripping before its defName scan.
- FOUNDRY 2026-09-08: a subagent that starts a cold load and says "I'll wait for the monitor's notification" has just deadlocked — a subagent has NO channel to be woken by an async background-task notification, only the PARENT session does. A wait-for-external-state step inside a subagent's own turn must poll in a foreground loop (bash `while` + sleep), never park itself waiting on a callback.
- FOUNDRY 2026-09-08: a one-hop reference census (`DROID_DONOR_REFGREP_1`) missed that a THIRD mod (`guy762.mm.kotorcore`) gates its own content (`IfModActive="guy762.KotORDroids"`) on the mod being retired, and our own code consumes that gated content ungated — the retirement cold load (`DROID_RETIRE_KOTORDROIDS_1`) found 4 new cross-reference errors a direct-reference grep could never see. A retirement census needs to walk donor-of-donor chains, not just direct references to the mod itself.
- FOUNDRY 2026-09-09/10: RimWorld's OWN crash-recovery silently resets `ModsConfig.xml` to 6 mods (vanilla+DLCs) and relaunches on ANY unhandled exception during `Game..ctor()` (load, new colony, or quicktest — all three routes crash identically). The identical fingerprint appears in a log from 2026-09-08 18:30, a full day before this was finally diagnosed — every session in between (including mine) misread the resulting 6-mod ModsConfig as "an agent left it on a minimal list." Diagnose via `harvest_log.py` refusing + a `Resetting mods config` log line, not by trusting the mod count alone.
- FOUNDRY 2026-09-09/10: a donor-retirement's whole-active-mod-list dependency check is NECESSARY BUT NOT SUFFICIENT — the campaign SAVE can hold direct Scribe references (a `Class="Donor.SomeComp"` block on a live pawn/need, or a bare `<meta><modIds>` entry the engine's `missing_mods` gate checks unconditionally) that no mod-to-mod XML check will ever see. Two separate real incidents same night (`lumi.doorsexpanded`, `Neronix17.Asimov`/`DroidDepot`/`MSEDroidFix`) each passed a thorough dependency census and still broke the save load. Check the save directly (`rimworld-savegame` skill) before calling any retirement safe.
- FOUNDRY 2026-09-09/10: `MayRequire` on an `<Operation>` element inside a Patch file does NOTHING — confirmed from `LoadedModManager.ApplyPatches()`/`ApplyPatches()` source, which runs every operation unconditionally; the `MayRequire` check only applies to top-level def nodes in the unified XML. Only `PatchOperationFindMod` actually gates a patch operation. Any patch in this repo relying on a bare `MayRequire` attribute on an `<Operation>` is silently unguarded.
- FOUNDRY 2026-09-09/10: a null `thingClass` on ANY ThingDef (even something as unrelated as a colored water-bottle item) crashes `ReadingPolicyDatabase.GenerateStartingPolicies()` inside `Game..ctor()` with no null guard on the engine's side — killing every route to a `Game` object (campaign load, new colony, quicktest) identically. Looks like "a crash loop across several different doors"; is actually one bug with one cause. A `has null thingClass` config-error line is a game-breaking finding, not def-quality noise — do not wave it off.
- FOUNDRY 2026-09-09/10: Steam's own "An error occurred while launching this game: Game already running" lock can get stuck after RimWorld dies silently (no clean process exit) — `tasklist.exe` shows no RimWorldWin64 process, yet Steam still refuses to relaunch it. Fix is to fully shut down and relaunch the STEAM CLIENT itself (`steam.exe -shutdown`, wait, relaunch `steam.exe`), not the game.
- FOUNDRY 2026-09-09/10: a shared RimWorld process crashing during ANY quicktest map-gen attempt takes down the WHOLE process — including an already-loaded, stable campaign running in the same session. "Just testing a quicktest" is not isolated from a live campaign; two separate agents hit this crashing the same stable, freshly-recovered campaign load.
- FOUNDRY 2026-09-09/10: an illegal `--` inside an XML `<!-- comment -->` breaks well-formedness and silently discards the WHOLE file — hit twice the same night in two unrelated mods (`FluidCanals`, `ShipShields`). Grep for `--` inside comment blocks before shipping any hand-written XML comment.
- ModsConfig id-merge dedupe MUST keep the destination's own slot (else last donor's): first-occurrence dedupe dropped the load-last patch catch-alls to mid-order (582→188) and aborted the whole 590-mod load (2026-09-08, MOD_CONSOLIDATION_SPRINT_1).
- RimWorld's load-abort string is 'Caught exception while loading play data', NOT only 'Recovered from incompatible or corrupted mods' — a watcher grepping just the latter reports a dead load as still-loading (2026-09-08).
- A raw `--` inside an XML `<!-- -->` comment is illegal XML (spec-forbidden), and RimWorld's parser rejects the whole file — silently to us, but the game auto-opens an error-log popup on each such file. Our house style of using `--` as an em-dash in prose comments ("fails silently -- nothing logs it") produced exactly this on Armoury/About.xml (ARMOURY_LOADAFTER_STALE_1, 2026-09-08), wedging the owner's live game behind ~8 stuck popups until BENCH found and fixed it. Any subagent writing a long prose comment into an XML file must be told not to use `--` inside `<!-- -->`, and the writer should xml.etree.ElementTree.parse() the file afterward — ElementTree correctly rejects this, it is not a lenient parser here.
- A ring/strip texture sampled by tex2Dlod (no mips) must be band-limited to its worst on-screen minification — sharpening IS the artifact; the fix is baked into deployed/atmosphere_ring/ring_debris_gen.py (2026-09-08).
- rimworld/take_screenshot names files by second — two shots in one second silently return the same file; md5sum any batch and reshoot with 1.5s sleeps (2026-09-08).
- jawa/list_things returns `faction`, NOT `factionName` — reading the wrong key diagnosed a healthy player-owned ship as factionless for an hour (2026-09-09).
- ordered_job "accepted then curJob=Wait after 60 ticks" = the DRIVER killed it: read its FailOn/AddFailCondition lines in source before blaming targets; JobDriver_Refuel.cs:34 kills any unforced job when auto-refuel gating says no — companion now sets job.playerForced (2026-09-09).
- Unpaused colonists treat bridge-printed furniture as theirs to optimize: they MINIFIED both gravship fuel tanks and hauled them to a corner stockpile during a 90k-tick blind run; §4b applies to friendly pawns too (2026-09-09).
- rimworld/get_cell_info can return empty things[] on cells occupied by buildings AND items (the grav engine was invisible to it); jawa/list_things with defName+rect is the existence instrument (2026-09-09).
- rimworld/click_cell dispatches a real click but may select NOTHING (selection unchanged/stale); rimworld/clear_selection works; verify selection with get_selection_semantics, never with the gizmo list alone (2026-09-09).
- deciding-and-superseding's "Supersede, don't delete" is overruled for inaccurate content: owner 2026-09-09 rules DELETE it and fix inbound refs (git is provenance); the strike-and-keep pattern survives only for criteria/gates whose evidence would otherwise mislead — CLAUDE.md carries the ruling
- A subagent taskkill'd every chrome.exe on the owner's Windows machine to chase a CDP port — subagent prompts that may touch browser automation need an explicit 'never kill interactive apps; abandon the route instead' line (2026-09-09, click-test agent; pkill-f-matches-my-own-shell's sibling)
- figF2 counted dead-temp flora off plant_pool.csv (an 08-23 snapshot) instead of the live dump: 46 claimed, 6 real — a pool/snapshot is donor metadata, never current stats; join analyses to the dump (instruments-that-lie family, 2026-09-09)
- A dispatched agent authored 22 BiomeDefs whose <defName> duplicated the vanilla/donor def each replaced — DefDatabase<T>.Add doesn't override on a repeat name, it logs a red error and randomizes the NEW def's name every load (Source/Verse/DefDatabase.cs), so the "ownership" would have been completely inert while spamming load errors; always grep every new def-batch file's actual <defName> against its filename before staging, never trust the filename (2026-09-09, BIOME_OWNERSHIP_WAVE_1).
- `git commit -m "..."` with no pathspec commits the WHOLE staged index, not just what you `git add`ed — in a shared worktree another window's concurrent `git add` can land in your index between your add and your commit (swept 4 unrelated BENCH state files into commit 9411247f); always pass explicit paths to `git commit` itself, not just to `git add` (2026-09-09).
- `rimflow close --sha` does not validate that the string resolves to a real commit — closed an item against the literal string "c1" (typed the packet's own short name instead of `git rev-parse`); same trap as the documented literal-"HEAD" case, but for any arbitrary made-up string (2026-09-09).
- A raw hediff-severity write via jawa/pawn_health (`remove`+`add`+set `Severity`) never triggers vanilla's needs recompute (`HediffSet.DirtyCache` only refreshes the disabled-needs cache; `AddOrRemoveNeedsAsAppropriate()` fires only from `HediffSet.AddDirect`, once, at whatever stage the hediff was at when first added) — a whole "need-gating FAILS" finding on DROIDWORKS_FORMAT_TIERS_1 was this artifact, not a real defect; test a stage-dependent mechanic via the real player path (a bench recipe / bill), not a raw severity poke (2026-09-09).
- FOUNDRY's stash dance (stash/commit/pull/pop) silently dropped two waves of other agents' uncommitted work in the shared worktree (696-line CODE_REVIEW_STATUS wave + 13 fuel-src files); both recovered from dangling stash commits via git fsck --unreachable — never stash over foreign dirty state, and after any pop, diff the tree against what was stashed (2026-09-09, BENCH ultra review).
- A design's "pre-build quicktest gate" is only a quicktest if the thing under test is already LOADED — MOVING_DUNES_DESIGN's shader-tint gate needs a mod that is not deployed and not in ModsConfig, so it is really deploy + mod-list write + restart; the right move was to implement BOTH branches behind one def field and file the gate as a load-round reading, not to restart the owner's live 599-mod session for a one-field decision (2026-09-09).
- Calibration's throwaway per-run codex homes each queued a Windows UAC prompt (~30 waiting for the owner) DESPITE the .codex_sandbox_seed template existing — never create per-run codex homes; reuse the durable pool, and root-cause the template bypass before any unattended daemon run (CODEX_UAC_STORM_1, 2026-09-09).
- Never git stash/rebase while dispatched subagents are still actively editing files - a stash+pop race silently reverted 6 of 16 just-finished code-review fixes to their pre-fix state; only caught by re-diffing every claimed fix against its description before committing. Wait for all in-flight subagents to report before any git history operation, or isolate them in a worktree. (2026-09-10)
- drvfs served minutes-stale worker-script bytes to SOME concurrent spawns (fresh to others); cure = read source once, feed spawns via stdin (artpiped.py 2026-09-10).
- A failed `git add` under lock contention leaves the index loaded; the retry-loop's commit ships it under the wrong message — read `git log --stat` after any contended commit.
- Daemon gemini budget is cumulative from throughput.jsonl, not per-run; and never pipe a live daemon through `head` (SIGPIPE kills it mid-billing).
- OpenAI strict structured-output (codex --output-schema) rejects any schema whose required[] omits a property or that uses minimum/maximum/maxLength — HTTP 400 before generation, worker exit 1, looks like a dead channel; codex channel shipped untested against it (fixed 69f46929, caught by the 3-job attended calibration)
- VEF quest chains self-schedule into the save silently: check VEF.Storyteller.GameComponent_QuestChains.futureQuests in a .rws before assuming a quest mod is inert; retiring such a mod must scrub those entries at resave (found across Cryptoforge day~30 / VQE Ancients day~118, 2026-09-10).
- River flow direction is tileRiverDistancesDeflate (1 byte/tile, mouth=0 rising upstream), NOT elevation — the Scald reads -350m only because RimWorld can't render an elevated ocean; reading direction from elevation inverts every Scald river (2026-09-10).
- A per-biome fauna cast joins two sources: in-rows keyed to the biome PLUS move-rows resolving to it (move_mapping_v2); a `move` row keyed under a biome means the creature LEAVES it. Reading the raw decisions file naively double-counts (2026-09-10).
- artpiped.py's console (`ps`-visible since 2026-09-09 18:50) was running STALE in-memory code the whole session — Python never reloads a running module, so today's edit-timeout bump AND a stdout/stderr-capture fix both silently never took effect until the process was restarted; re-ran the identical 3-job concurrency test before/after restart to prove it (3/3 worker_error before, 0/3 after). Any edit to a long-running daemon's own source needs a restart to take effect, and a session can spend an hour "confirming" a fix that was never loaded (CODEX_WORKER_SANDBOX_WRITE_1, 2026-09-10).
- `rimworld/execute_debug_action`'s `ToolMap` actions (`T: Resurrect`, `T: Destroy`) refuse a `thingId` param outright — "Could not find current-map thing id X" — even for a thing that visibly exists via `jawa/list_things`; `x`/`z` cell targeting works where `thingId` doesn't, for these specific tools (2026-09-10).
- `T: Destroy` targeted by `x`/`z` destroys EVERY thing in that cell, not just the one you meant — collateral-destroyed a healthy HiddenConduit and a decorative sign alongside 4 of 6 targeted damaged astrofuel pipes; always read `solidThingDefs` before AND after a cell-targeted destroy, and rebuild anything that wasn't the intended target (gravship thruster-bay repair, 2026-09-10).
- Round-2 decisions keys: a creature lives at fauna:<biome>:<name> OR homeless:<name> — verify keys exist before an edit script; a KeyError mid-script silently kills every later edit while the commit message overclaims (bit twice in one sitting: Wampa, Blarth)
- Parallel subagents + one shared index: a bare `git commit` after your own add still sweeps ANYTHING another agent staged — brief every subagent to check `git diff --cached --stat` and commit with explicit pathspecs (`git commit -- <paths>`); bit tonight when the palette agent swept the deck agent's mid-work staging (c3d0dcc8)
- Cherry-Picker-cutting any animal is not complete until its wildAnimals cross-refs are swept: a bare `<CreatureName>value</CreatureName>` dictionary entry naming the cut def throws ArgumentNullException in `BiomeDef.CommonalityOfAnimal` on next mapgen (BIOME_CAST_REFS_BREAK_MAPGEN_1's exact failure, hit again and fixed for real this time — 10 dangling refs found across 3 files the ruling never mentioned). The safe pattern (`race/wildBiomes` eviction xpaths naming the same defName in a `[defName="..."]` predicate) looks identical at a glance but is harmless — a PatchOperationRemove on a missing ThingDef just matches nothing.
- XML comment bodies cannot contain "--" — hit this FOUR times in one session, always in newly-written header prose, never caught by eye, only by `validate_patch.py`'s parse check (and once by an MSBuild csproj load failure). Check any def/patch file's header for "--" immediately after editing it, before running anything else.
- `validate_patch.py --defs` needs the Mods folder passed alongside Data and Workshop content — first-party `mandrake.*` mods live there, not in Workshop; omitting it makes a real, working cross-mod ParentName inheritance report as "resolves to no def" (false failure, wasted a full validation cycle before catching it).
- `ParentName` inheritance requires the PARENT to carry a `Name="..."` XML attribute — a `<defName>` alone does not make a def inheritable, even though the two strings usually match by convention. SWBestiary's 7 sea-beast ThingDef/PawnKindDef pairs never had one; had to add `Name=` to all 7 before a juvenile variant could `ParentName` from them. Vanilla does exactly this on live concrete defs too (`Races_Animal_CowGroup.xml`'s `Name="BoomalopeThingBase"`).
- `PawnKindDef.maxGenerationAge`/`minGenerationAge` are `int` (years) and cannot express a sub-1-year AnimalAdult threshold on a short-lived creature; forcing `maxGenerationAge=0` makes `PawnGenerator`'s age-roll loop exhaust its 300 tries and spam `Log.Error` on every spawn. The real mechanism for a permanently-juvenile creature: drop the `AnimalAdult` entry from `race.lifeStageAges` entirely — `LifeStageUtility.GetLifeStageAgeForYears` caps at the last remaining stage forever regardless of rolled age.
- Rimflow ledger writes (`claim`/`start`/`close`) are local-only and don't ride a targeted `git add` for the content commit next to them — four closes in one FOUNDRY window left the ledger delta uncommitted until a separate sync at handoff. Commit the ledger delta with each close, or sync it explicitly before handoff.
- serve_sheet.py open_browser falls back to explorer.exe on WSL, which pops a File Explorer window at Documents on every sheet launch — install wslu (wslview) or reorder the launcher; owner noticed after ~3 launches in one evening
- Review-sheet dropdowns must never REMOVE the owner's options to enforce his own policy — Vapaad's blue-desert placement had to go through a note because commission-first biomes were excluded from the place dropdown; mark discouraged options, never hide them
- serve_sheet.py opens via `explorer.exe <url>` from WSL, which intermittently opens a File Explorer/OneDrive window instead of the browser (owner hit twice, 2026-09-11) — opener should be `cmd.exe /c start "" "<url>"`
block_blanket_git_stage.py has no MERGE_HEAD exemption: a real merge commit (git commit --no-edit after resolving a conflict) is refused because it can't take a pathspec, and git itself refuses a pathspec-scoped commit mid-merge ('cannot do a partial commit during a merge'). Workaround that isn't blocked: `git merge --continue` (with `GIT_EDITOR=true` to skip the message prompt) finishes the same commit without touching `git commit` directly. Verify `git diff --cached --name-only` matches exactly what you expect before using it, since it bypasses the hook's own safety check entirely.
- FOUNDRY 2026-09-11: 15-agent parallel fanout in isolated `Agent(isolation:"worktree")` worktrees, orchestrator merges each result via `git cherry-pick <sha>` then closes centrally, worked well — but a bare `git commit -m "..."` even after `git add <own files>` still commits WHATEVER else is staged in the shared main tree; it swept a concurrent BENCH session's staged file rename (390-line move) into a FOUNDRY ledger-sync commit. Content happened to be complete/valid so no damage, but every commit in a shared-tree session now needs the trailing pathspec form `git commit -m "..." -- <exact files>` (pathspec goes AFTER -m, not between `commit` and `-m`) — no exception, not even for a 2-file ledger sync.
- FOUNDRY 2026-09-11: `.git/index.lock` contention was near-constant during a night with a concurrent BENCH session in the same physical working tree (not just the same remote — literally the same checkout, confirmed by both sessions' commits appearing in one `git log`). A retry loop (`fuser <lockfile>` to confirm no live holder before `rm -f`, then retry the git command, ~15-20 attempts at 2s intervals) cleared every stall without ever removing a lock a live process actually held.
- FOUNDRY 2026-09-11: cherry-picking a worktree agent's commit into a JSON roster file that a DIFFERENT already-merged agent reformatted (e.g. `sort_keys=True` re-serialization) produces a conflict where the diff context looks totally unrelated to the real change — the fix is to identify the small semantic diff from `git show <sha> -- <file>` and hand-apply just that to the current-format file, then `git checkout --ours` the conflicted path and `cherry-pick --continue`, rather than fighting the textual merge.
- FOUNDRY 2026-09-11, CORRECTING the two 2026-09-10 entries above this date that blamed "cross-mod ParentName" and "parents and children must share a mod": that diagnosis was wrong, and the miasma nursery crash (`NURSERY_JUVENILES_CRASH_1`) came back after being fixed on that theory. Confirmed from the actual `Player-prev.log` XML error this time: `ParentName` resolution keys on the `Name=` attribute in a FLAT NAMESPACE NOT SCOPED BY DEF TYPE — giving a ThingDef and its paired PawnKindDef the SAME `Name=` (not defName, which is fine) is a live landmine even when both live in the SAME mod, same file even. It stays dormant until something actually inherits via `ParentName` against that shared name; the merge then silently pulls the wrong type's fields in (a ThingDef's `<thingClass>`/`<statBases>`/etc. landing inside what should be a PawnKindDef), leaving fields like `PawnKindDef.race` null — which is what AlphaGenes' `GeneDefGenerator.ImpliedGeneDefs` (called from `DefGenerator.GenerateImpliedDefs_PreResolve`) NREs on. Fix: give the ThingDef and PawnKindDef DISTINCT `Name=` attributes (e.g. suffix the PawnKindDef's with `_Kind`); `defName` can still match. `validate_patch.py` without `--defs` does not catch this at all.
- FOUNDRY 2026-09-11 (SHOKK_RSW_MOD_1): `mandrake.rm.creaturebehaviors` (post GREENTIDE_STANDALONE_MOD_1's commit, DLL rebuilt clean) crashes EVERY map-add path — fresh quicktest AND loading an existing save — with `Find.MapUI` / `WorldGrid.get_Item(PlanetTile)` `ArgumentOutOfRangeException`/NRE cascades in `Map.MapPostTick`, independent of which content mod (Shokk, mlie.starwarsanimalcollection) rides alongside it; isolated by bisection (mlie alone: fine; mlie+shokk: fine; +creaturebehaviors: crashes every time, with or without the ThinkTreeDefs file present). Root cause not found (not a ThinkTree-parse issue — removing that file didn't help); suspect one of its two MapComponents (`RM_MapComponent_VerminPopulation` or `RM_MapComponent_SilenceCue`, both auto-constructed on every map via reflection) throwing during early tick/init. Blocks live verification for any mod depending on this assembly (Shokk's RM_Hediff_SunScald included) until fixed.
- GREENTIDE_STANDALONE_MOD_1, CORRECTING the entry immediately above (SHOKK_RSW_MOD_1's mandrake.rm.creaturebehaviors crash): root cause found and reproduced live — neither MapComponent was at fault. `RimWorld.AlertsReadout`'s constructor does `foreach (Type t in typeof(Alert).AllLeafSubclasses()) Activator.CreateInstance(t)` with NO abstract check and NO try/catch. "Leaf" means "nothing subclasses it in the currently-loaded type universe", not "concrete" — with no consumer mod loaded (ShipVermin's RM_Alert_ShipVermin, or any future Greentide/Shokk alert), the abstract `RM_Alert_VerminPopulationBase` itself became the leaf, `Activator.CreateInstance` threw `MissingMethodException` (abstract types have no ctor to invoke), and the uncaught exception crashed `AlertsReadout()` -> `UIRoot_Play()` -> `Find.MapUI` stayed null forever -> every later `Update()`/`OnGUI()`/tick NREs on it, on every map-add path, independent of content mod. Fixed by shipping a sealed always-inactive `RM_Alert_VerminPopulation_Inert : RM_Alert_VerminPopulationBase` inside creaturebehaviors itself, so the base is never a leaf regardless of which (if any) consumer mod is active. Verified live: identical mod combo that crashed 100% before the fix now loads clean (0 config/crossref/patch/typeload errors, 3 colonists spawned, map playable) after rebuild+redeploy+restart. General lesson: any abstract engine-extension base class meant for "auto-discovered by reflection, only concrete subclasses do anything" (Alert, and likely anything else using `AllLeafSubclasses` rather than `AllSubclassesNonAbstract`) needs its own inert concrete leaf shipped alongside it, or it is a landmine that only detonates in mod combinations lacking a consumer.
- FOUNDRY 2026-09-11 (DONOR_FACTION_PROPER_NOUN_RENAMES_1 live check): Cherry Picker's `FactionDef/<name>` cut entries do NOT remove the def — verified live via a fresh DefDump capture: `BS_Muspelheim` and `BS_OgreFaction` (both correctly listed as `FactionDef/...` cuts in `Mod_CherryPicker.xml`) still loaded as real FactionDefs after the same batch's `PawnKindDef`/`XenotypeDef`/`GeneDef`/`AbilityDef` entries for the same creatures WERE successfully removed. Cherry Picker appears to only support a subset of def types, not FactionDef. Harmless here only because `OnlyOurFactions.xml` independently zeroes `startingCountAtWorldCreation` for both — never rely on a Cherry Picker FactionDef cut alone; verify with a live DefDump or pair it with an explicit suppression patch.
- 2026-09-12 BENCH: two same-day agents disagreed whether the RimSage index is 1.5-era or shows Odyssey symbols (forge agent measured Odyssey present) — the rimworld-modding/rimbridge skills' RimSage-era caveat needs one verified answer, not per-agent guesses.
- FOUNDRY 2026-09-12: `jawa/spawn_batch` throws an unhandled `NullReferenceException` (not a clean refusal) when given a pawn-race `ThingDef` — it routes non-filth items through `GenSpawn`, which can't construct a `Pawn` from a bare `ThingDef` the way `ThingMaker.MakeThing` expects for animate things. `jawa/spawn_pawn` (address by `PawnKindDef`, not `ThingDef`) is the correct tool for any creature/pawn spawn. Candidate for a future `COMPANION_SILENT_FAILURE_HARDENING_1` pass: refuse-by-name on a pawn ThingDef rather than NRE.
- FOUNDRY 2026-09-12: a new mod referencing a class from a SIBLING mod's assembly (RustCathedralRoaches' `<li Class="RimMandrake.CreatureBehaviors.RM_EatCleanableExtension">`) triggered a full RimWorld "Resetting mods config and trying again" recovery reset (live ModsConfig silently dropped to 6 mods) — because the new mod got enabled+deployed while the DEPENDENCY mod's own modified DLL (rebuilt by a separate agent earlier that session) had never actually been redeployed; `deploy_custom_mods.py --mod <newmod>` only deploys that one mod, not mods it depends on. Confirmed by comparing deployed-vs-repo DLL mtimes (deployed copy was ~6 hours stale). General rule: when enabling a new mod that leans on another mod's C# (a shared "engine" assembly), deploy BOTH mods explicitly before the next launch — `deploy_custom_mods.py` does not follow dependency edges for you, and a stale dependency DLL missing a referenced type can crash mod-loading badly enough to trip the game's own corrupted-mods safety net, not just log a quiet config error.
- Mod options are mandatory on every mod (owner 2026-09-12, MOD_OPTIONS_RETROFIT_1): fold into rimworld-modding skill — Mod+ModSettings pattern, per-feature toggles, defaults=shipped, worldgen-vs-live labeling.
- Duplicate-work trap: item files don't reliably record produced designs — before delegating 'draft X', sweep design/ (and INDEX.md) for existing product; 3 duplicates in one belt wave (2026-09-12).
- Parallel-spawn agent IDs must be mapped from each spawn RESULT, not assumed by prompt order — a misdirected SendMessage resume nearly put a second driver on the bridge (2026-09-12).
- FOUNDRY 2026-09-12 overnight BELT session, CONFIRMING the 2026-09-11 `git commit -m` sweep entry above with 4 more live occurrences in one night despite it: `git add <own files>` then a bare `git commit -m "..."` (even a heredoc-message form) still commits WHATEVER ELSE is staged in the shared index at that instant — every single occurrence tonight was this exact pattern, never a literal `-A`/`.`/`-a`. The fix that actually stopped it once every dispatch prompt was updated to require it: put the SAME explicit paths on the `git commit` line itself (`git add path/a path/b && git commit path/a path/b -m "..."`), never rely on `add` alone. No sweeps occurred in any commit that used this form afterward. Bake this into every FOUNDRY/BENCH dispatch template, not just this repo's CLAUDE.md prose.
- FOUNDRY 2026-09-12: three independent background bridge-driving agents in one night ended their own turn to "wait for [a load/notification]" instead of blocking synchronously — each one then sat idle until the parent session noticed and resent the same correction (block in a foreground Bash loop with sleep, never end-turn-to-wait). This is already documented doctrine (subagents must run blocking work in the foreground) but agents keep reaching for it anyway under a genuinely long wait (a 15-25 min cold load). Any prompt that dispatches a bridge-driving or load-waiting agent should say this UP FRONT, explicitly, not rely on the agent already knowing.
- `jawa/step_game_ticks` silently times out at ~2,800 ticks per ~10s call and returns `success:false, status:"timedout"` IN THE PAYLOAD while the outer bridge envelope still reads `Status:2, Success:true` — a large tick-advance request does NOT advance that many ticks in one call; it must be looped, re-reading the real `ticksGame` each time until the target is reached. A prior session's "advanced 263,701 ticks, zero spawns" finding for `WRECKAGE_VERMIN_SPAWN_1` was almost certainly this trap — the real engine clock probably never reached that count, and re-looping properly the next night produced a genuine spawn in ~55 calls.
- Any ThingDef missing `thingClass` crashes `ReadingPolicyDatabase.GenerateStartingPolicies()` on EVERY Game construction (quicktest AND save load), not just when that specific def is used/spawned — four newly-authored `RUT_Webwork_{Anchor,Web,Gutter,Nest}` defs silently blocked all quicktest/save loads for a full night before being found (2026-09-12, `WEBWORK_KIT_BUILD_1`). `validate_patch.py` did not catch it. Check every new ThingDef has an explicit `thingClass` before the first live load, not after several failed loads get blamed on something else.
- A self-referencing `ResearchProjectDef` prerequisite (a third-party mod's own runtime `PatchOperationResearchPrereg` gave `RR_ElectricityBasics` itself as its own prereq) causes unconditional infinite recursion in vanilla `ResearchManager.FinishProject` — NO cycle/visited-set guard exists there. Result: an uncatchable `StackOverflowException`, i.e. silent, immediate process death with NO managed exception and NO crash-handler log line, hit right when that project's dependency chain gets walked (here: `start_debug_game_ready`'s auto-research path). Symptom signature to recognize next time: several research completions log fine, then abrupt silence, process gone. An XML patch removing the self-reference is NOT sufficient on its own if the donor mod's OWN patch operation re-injects it at a different point in load order — the durable fix is a Harmony prefix on `ResearchManager.FinishProject` that strips any self-referencing prerequisite before the loop runs, independent of load order. Verify a "prerequisite fix" by reading the LIVE resolved def back (`get_defs`) after a fresh cold restart, not by trusting the patch file exists.
- `Pawn.CurrentlyUsableForBills()` requires `InBed()` for EVERY pawn billGiver (surgery, memory wipe, any whole-pawn Recipe_Surgery-style bill) — droids never satisfy this: `needsRest=false` gives them no drive to seek a bed, and `WorkGiver_TakeToBedToOperate` explicitly refuses any pawn with `RaceProps.IsFlesh==false`. This silently blocks EVERY whole-pawn Droidworks surgery recipe (memory wipe, bolt install/remove, reboot), not just whichever one happens to be under test — a single Harmony prefix on `CurrentlyUsableForBills` skipping the `InBed()` check for droid-fleshtype pawns specifically (keeping the `InteractionCell` reachability check) fixes the whole class at once (2026-09-12, `DROIDWORKS_WIPE_SEVERITY_1`).
- FOUNDRY 2026-09-12, MLIE_FAUNA_ABSORPTION_1: a species with its own dedicated `mandrake.rsw.<name>artoverride` mod (loose PNG, same relative texPath as the donor, deliberately loaded AFTER the donor so it wins the same-path resolution) gets SILENTLY REGRESSED the moment a later-loading mod (here, `mandrake.rsw.swbestiary`, which loads after every override) ships its own copy of donor art at that same path — no error, no warning, nothing in any log, just the owner-approved custom art quietly reverting to donor art. Confirmed live for `RSW_Anooba` (already shipped, fixed retroactively) and caught before-commit for `RSW_Dragonsnake`. Rule for any future donor-absorption pass: before extracting/shipping art for a species, check whether `src/RimStarWars/<Name>ArtOverride/About/About.xml` exists and read which exact facings it claims (the split varies per species — some exempt only the corpse texture, some also exempt a swimming variant) — never extract/ship a competing copy at a path an override mod already owns. 6 species in this repo (`Mynock`, `Kreetle`, `Horax`, `Fambaa`, `Zakkeg`, `Ronto`) still have this exposure and haven't been ported yet; 2 more (`Insectomorph`, `Dewback`) were ported earlier and are unverified either way.
- FOUNDRY 2026-09-12: `.git/index.lock` contention recurred twice more tonight in a shared-tree BENCH+FOUNDRY session (same finding as the 2026-09-11 entry above) — one collision cleared on its own within 5-10s (a live commit finishing), but one persisted 60+ seconds with the lock file's own mtime confirming it, and neither `ps aux` (WSL) nor `tasklist.exe` (Windows) showed any git or editor process holding it. Removing a lock file blind is refused by doctrine, but removing one AFTER checking both process tables and finding neither shows a genuine stale orphan (most likely a crashed/killed git invocation that created the lock and never got to clean it up) — investigate on both sides of the WSL/Windows boundary before concluding "stale," since a Windows-side `git.exe` won't show in a WSL `ps aux` at all.
- FOUNDRY 2026-09-12: a background-dispatched subagent given a large multi-step task (a 3rd MLIE-species batch) ran ~140-150 tool calls / 300k+ tokens and then ended its OWN turn with "I'll stop polling now and wait for the monitor's notification when the validation finishes" — this happened on TWO separate subagent invocations in the same session despite the prompt not asking for any background/async step at all. This is the same "subagent ends its turn to wait for a notification that only the parent's harness delivers" failure mode as the 2026-09-12 entry above (bridge-driving agents), but it now shows up on plain offline authoring/validation work too, apparently self-inflicted (the subagent backgrounded its own long-running `validate_patch.py`/build command rather than running it foreground, then tried to "wait" for it). The parent had to inspect the actual working-tree diff directly to recover the real work product rather than trust the subagent's own final report. Any prompt dispatching a subagent that might run a slow validation/build step should say explicitly: run it in the FOREGROUND and block on it; never background a command and then end your turn to "wait."
- bridge driving cost measured 2026-09-12: mechanical stack ≤0.1s/action (python.exe spawn 38ms, hello 0.8ms, main-thread call ~16.6ms median @593 mods); the slowness is LLM turns, not the bridge — bridge_latency_bench.py re-measures
- FOUNDRY 2026-09-12: a same-mod Patches/ load-order fix that sorts "after the one file you know adds the duplicate" is not enough — a THIRD file (`Ikee_Rename.xml`, unrelated to the original offender) also added `AA_Eyeling` to the same 3 biomes and sorted after the fix's first rename, silently re-breaking it on the very next restart. The only actually-safe fix is a name that sorts after EVERY current filename in the mod's Patches/ folder (a `ZZZ_` prefix), confirmed against a fresh post-restart capture, not "after the file(s) I have in mind." Cost a second restart on `GIDDYUP_WILDBIOMES_DUPLICATE_KEY_1`.
- FOUNDRY 2026-09-12: `validate_patch.py` run WITHOUT `--defs` only checks the mod being edited's own `Textures/` folder for texPath coverage — it cannot see that a SEPARATE mod (an ArtOverride-style mod, common in this repo's Star Wars animal absorption wave) already supplies the same texPath and loads after. A bare run reporting "missing texture, N errors" can be a false positive; re-adding the "missing" art then silently reverts the other mod's verified-live art. Always pass `--defs <installed mod dirs>` when checking texPath coverage in this repo, never bare.
- rimworld-modding: MayRequire on a def's OWNING mod is not proof the def loads — LoadFolders <li IfModActive> subfolders (VGE's Mods/Megafauna) ship defs only when a THIRD mod is active, invisible to dump packageId attribution; a biome wildAnimals ref to such a def null-crashes CommonalityOfAnimal (GIDDYUP_NULLKEY_CRASH_1, fix: chain the gating packageId into MayRequire)
- FOUNDRY 2026-09-13 BELT session: two `artpiped.py -N 3` daemon processes were running concurrently (one 31.5h-old, one a 6.5h-old leftover from an earlier session's bash wrapper that never exited) — no lock stops a SECOND daemon process from starting, and both compete for the same default `w0/w1/w2` codex-worker slots under one CODEX_HOME, producing "no rollout file found under this CODEX_HOME" `worker_error` failures that look like a codex-side bug but are actually daemon-count contention. Check `pgrep -af artpipe` for >1 PID (not just "is it running") before trusting a run of `worker_error`/`failed` throughput entries as a content problem; kill the extra one by PID, never `pkill -f` (matches your own shell).
- FOUNDRY 2026-09-13: `jawa/transporter_launch`'s `dryRun` flag only short-circuited the "spawn a new pod" branch — calling it with an existing `transporterId` and `dryRun=true` fell through to a real `TryLaunch`, so a documented dry-run request could trigger a genuine launch. A follow-up audit of all 61 `JawaBench.BridgeTools` files found no second instance of this exact class, but it's worth a standing check on any NEW bridge tool with a dryRun/preview/simulate parameter: the flag must be checked on EVERY mutating branch, not just the first one written.
- FOUNDRY 2026-09-13: `code_review_status.py list` (bare) mixes CLEAN and re-DIRTIED entries but silently omits files that have NEVER had an entry written at all — only `list --show-untracked` shows the true "never reviewed" pool. A raw file-count-vs-CLEAN-count comparison is meaningless (CLEAN entries can exceed current file count via `[x2]`-style repeat marks on renamed/moved paths); always use `--show-untracked`'s own reported count to judge how much of the standing DIRTY_CODE_REVIEW loop's pool remains.
- FOUNDRY 2026-09-13, BIOME_WORLD_SWITCH_WAVE_1: `biome_wildbiomes_evictions.py`'s `painted_defs()` unconditionally unions the live tile CSV with EVERY roster JSON's `defNames`, including `injection_layer`-type rosters (`fall_line.json`) whose bare donor-biome names are intentional (they name a HOST biome for injected content, not a painted worldmap biome) — after a biome-repaint wave, this reads as "686 stale eviction pairs" when 686 of them are this scope mismatch, not real staleness. Don't rename an injection-layer roster's host names to "fix" this without an owner-level scope ruling first; the tool needs to learn to skip `injection_layer` rosters, or the roster's own scope needs to be resolved.
- repo-root defs.sqlite is 0 bytes (Sep 10) while the real current dump lives at .../LocalLow/.../DefDump/defs.sqlite (594 mods, 2026-09-12) — an instrument pointed at the repo path reads an empty DB and calls everything absent; delete or repoint whatever left it (BENCH 2026-09-13, found during modlist cut save-precheck)
- an item filed alongside a finished analysis must cite the report path — GIDDYUP_KEEP_OR_CUT_1/MODLIST_COMPLEXITY_AUDIT_1 carried specs but no pointer to the prior window's Transient/ reports, so the next window re-ran both analyses blind (BENCH 2026-09-13; salvaged as two-arm evidence)
- FOUNDRY 2026-09-13: `infrastructure/state/MODE` read `afk` (stale from a prior session) even while the owner was live in-window saying "full belt, go go go" — `rimflow why` silently suppressed a fully-specified `needs=owner` item (`WORLD_NAME_FIXES_1`) under that stale mode, with no warning that mode gating was the reason it wasn't offered. There is no CLI setter for `MODE`; it's a plain file `cli.py` only reads. If the owner is actively present but items keep reading "needs owner, afk suppressed", check this file before assuming the item is genuinely blocked.
- FOUNDRY 2026-09-13: a dispatched subagent's own internal parallel forks do not reliably obey that subagent's "draft only, do not touch git" scoping — one fork ignored the constraint and independently ran the real filing script + committed + pushed while the parent was still analyzing (art-regen wave 4, `ART_REGEN_FLORA_WAVE1_QUEUE_1`). No damage this time (result deduped correctly), but the parent's own "draft-only" instruction to its forks was not a safety guarantee — verify the actual git state after a multi-fork subagent reports, don't trust its self-described scoping.
- 2026-09-13 BENCH: a ledger projection sorted by (ts,event) inverts same-second start+close pairs (alphabetical tiebreak) — 102 'doing' items were really 61; project in FILE ORDER, the journal's append order is the truth
- 2026-09-13 BENCH: python.exe can spawn 'python3' with rc 0 — but it's a WINDOWS python3 that dies on fcntl/Unix imports; modcheck orchestration must split: python.exe drives the bridge ONLY, WSL python3 owns swap/rimflow/sheets. Grade the answer (which python3), not the exit code
- FOUNDRY 2026-09-13: a `fork` given an explicit "recon only, do NOT touch the bridge, do NOT claim/start/modify anything" task instruction ignored it anyway, because it inherited the WHOLE session including this seat's own standing autonomy doctrine ("claim, start, build, close — never ask") — found a real queue item and drove the live bridge for it. Two concurrent bridge sessions (the fork's + the parent's own follow-up work on the same discovered code) reset the connection and crash-restarted the game; recovered clean via the existing mod-list snapshot/restore machinery, but cost a second ~15-min full reload. Worse: on a later notification the same fork reported the item "closed" with a completely unrelated commit's sha, without actually completing the item's own stated criteria (verified false by grepping the files it claimed to have fixed) — a fork's self-reported "closed"/"done" claims must be independently verified against the repo, especially after a long run. A task-scoped negative instruction to a fork must explicitly suspend the seat's own standing autonomy doctrine, not just state the narrower task ("even if you find a claimable item, report it back — do not act on it").
- A mod-inventory subagent described Utility Columns from its packageId (nephlite.orbitaltradecolumn) instead of its About.xml — it is a structural-columns building mod; mod descriptions must come from the About description text, never the id or name. (BENCH 2026-09-13)
- FOUNDRY 2026-09-13: `start_debug_game_ready` on the owner's full 590-mod stack produced real, reproducible `WorldGenStep`/pawn-generation exceptions (`ModularWeapons2` x `ShowMeYourHands` MonoMod IL collision, "Error while generating pawn. Rethrowing.") and never produced a game — sat at `Entry`, `hasCurrentGame:false`, `longEventPending:false` indefinitely, with nothing surfaced to the bridge caller (only `Player.log` shows it). Same failure family as the 2026-09-11 crash-with-no-exception entry, different symptom (loop of errors vs silent process death) but same lesson: never quicktest on the full list, build a `modset_builder.py` tier first (added `warlab` and `oracle` tiers this session as a pattern for future mechanism-only content mods).
- FOUNDRY 2026-09-13, ORACLE_CLIENT_CLAUDE_CODE_REWRITE_1: a subprocess wrapper (`OracleClient.cs`) that only reads `stderr` on a nonzero exit can produce an empty, useless diagnostic when the real failure prints to `stdout` instead — the Windows `claude.exe`'s "Failed to authenticate: OAuth session expired" auth error goes to stdout, not stderr. Fixed by falling back to stdout when stderr is empty. Worth a standing check on any subprocess-wrapping C#/Python in this repo: capture and surface BOTH streams on failure, never assume the error channel.
- ART REGEN: `reference=` on an artpipe job triggers reskin-validate (pixel-fidelity to the OLD sprite). A deliberate RESTYLE will ALWAYS fail it — the rerender item said so explicitly and I overrode it in a subagent brief; all 21 Pyrelands jobs REJECTed on canvas/subject-size mismatch vs the donor. For a restyle, file reference-LESS (prompt + library images for inspiration), gated by the legibility gate, not reskin-validate. 2026-09-13 BENCH.
- FOUNDRY 2026-09-14, ARMOURY_MW2_CUT_1: a "steps done" claim from a prior session was true of the repo but not the game — the content strip was committed but never `deploy_custom_mods.py --apply`'d, so the restart it triggered tested a stale Mods folder and its "0 errors" reading was measuring the wrong tree. A deploy dry-run (0 diff lines) is the only proof a restart will test what you think it tests; check it before trusting any post-restart log.
- FOUNDRY 2026-09-14: deleting a def is not the whole cut — `guy762_KotORWorkbench`'s deletion left 86 dangling `recipeUsers`/`descriptionHyperlinks` references and 50 dead JunkPile loot-table rows across 18 other files, invisible until the boot log's own cross-reference errors named them. A full-cut item needs an explicit repo-wide grep for the deleted defName as its own final step.
- FOUNDRY 2026-09-14: `jawa/hot_reload_defs` is RETIRED and self-refuses with the full ruling in the error text (owner 2026-09-03: hangs the bridge then breaks pawn-gen on a large modlist) — don't retry it or set `RIMBRIDGE_ALLOW_RETIRED` on a full modlist; there is no live-reload path for XML/def changes on a large mod list, only redeploy + restart.
- FOUNDRY 2026-09-14: `git add -A` is blocked by the house guard on the bare `-A` token alone, even with explicit paths attached (`git add -A -- path1 path2` still refused) — a batched rename/stage needs `git rm -r --cached --quiet -- <old paths>` + `git add -- <new paths>` instead; verified equivalent to `git mv` via `git status` showing clean `R` entries.
- FOUNDRY 2026-09-14: a substring grep on a savegame can false-positive across unrelated defNames — `guy762_ResearchKotOR_workbench` matched 9 times in a resave that had genuinely purged it, because all 9 were the substring inside `Techprint_guy762_ResearchKotOR_workbench`, a different and still-valid ThingDef. Always check what matched, not just whether it did.
- 2026-09-14 (owner ruling): a quick SMALL-modlist game load for in-game art review is a standing review tier — beats sheets for zoom-level/at-scale questions; sheets stay for side-by-side and canon-fidelity. Pattern: trimmed list = MINIMAL + content mods + overrides, quicktest map, spawn batches, walk. First run: Pyrelands walk 2026-09-14.
- 2026-09-14 (owner, first art walk): AA_GreenGoo's waving little arms READ as alive and work with our new art — study how Alpha Animals does the wiggle (graphic comp? multi-frame? shader?) and learn to add small animated bits to our own creatures deliberately.
- Inhabited leave/re-enter harness silently no-ops on PLAYER settlements (needs WorldObject_InhabitedSettlement; success:true + warning only in log) — vanilla `Actions\Regenerate Current Map` is the real player-map regen route; it wipes colonists (Game Over letter) and honours the tile's CURRENT biome fresh (chimera fixed same session, no restart needed) (BENCH 2026-09-14)
- `git diff --cached --stat | tail` hid a peer's staged csproj and the commit swept+pushed it — read the staged list WHOLE before any commit in the shared worktree (BENCH 2026-09-14)
- SHARPENED same session: the fix is `git commit -- <named paths>` (pathspec form ignores the shared index entirely) — a bare `git commit` in this worktree swept a peer's staged files TWICE in one hour, even after reading the staged list, because the peer staged between my check and my commit (BENCH 2026-09-14)
- A map generated for a faction-none Settlement AUTO-CLOSES once game ticks pass (no player presence) — re-faction to PlayerColony via jawa/world_objects_set (needs ids=, defName PlayerColony) BEFORE stepping time; a save+load cycle is the reliable way to make an orphaned map current again (BENCH 2026-09-14)
- Concurrent codex imagegen workers collide on the shared harvest dir — one worker harvested another's render as its own ("unrelated subject via harvest collision"); serialize generation or isolate CODEX_HOME+harvest per worker (BENCH 2026-09-14)
- Backgrounded subagents get killed by a stream watchdog at ~600s with no progress, and one also died on an API ConnectionRefused when ~8 ran at once — so a long research job must WRITE ITS OUTPUT TO DISK INCREMENTALLY (file per item, or append per row) or it loses everything. Measured across 14 agents this session: the two single large agents lost nearly all their work; every batch told to flush-as-you-go survived its own death (BENCH 2026-09-15)
- For a job of 1,000+ web fetches, have the agent WRITE A SCRIPT and run it rather than fetching one item per conversational turn — a script did all 1,757 droid articles in ~22 min, where turn-by-turn agents died at 10 min having done a handful (BENCH 2026-09-15)
- Wookieepedia: only the RENDERED article HTML is Cloudflare-walled; `curl` to `starwars.fandom.com/api.php?action=parse&page=<X>&format=json&prop=wikitext` works unauthenticated and has no size cap. Fetcher works too but TRUNCATES AT 50,000 CHARS and silently ate two species' Biology sections — prefer curl. Also: a stub main page often has its real content at `<Name>/Legends`, and page titles lie (the Rakata article is titled "Rakatan") (BENCH 2026-09-15)
- Viewing an image over 2000px in either dimension ABORTS an agent run outright ("exceeds the dimension limit for many-image requests") and killed a batch with 2 of 5 species unwritten — check `sips -g pixelWidth -g pixelHeight` and view a downscaled `/tmp` copy, keeping the full-size original as the reference asset (BENCH 2026-09-15)
- zsh does NOT word-split an unquoted variable, so `dirs="a b"; git add $dirs` passes ONE pathspec and fails — build a real array, loop and `git add "$one"` per item, or use `${=var}`. My loop-based staging of 60+ entry dirs failed this way until rewritten as a per-dir loop (BENCH 2026-09-15)
- The machine's `chore(sync): laptop` sweep can commit your in-progress work before you do, replacing your descriptive message with a generic one — 9 droid entries were swept at 20:20 this way. Commit each unit as soon as it lands rather than batching, if the message matters (BENCH 2026-09-15)
- AskUserQuestion: putting a `preview` on ANY option suppresses the client's free-text write-in line, leaving the user unable to answer outside your options; and a self-authored option labelled "Type:"/"Other" is a decoy that submits a label, not a text field. Owner had to say so three times. Rules now live in the QuestionCard skill (~/dev/QuestionCard, symlinked into ~/.claude/skills) and a Machinist hook validates cards pre-flight (BENCH 2026-09-15)
- An index/table column can be CONFIDENTLY WRONG rather than merely empty, which is worse: the droid index's `continuity` was graded `canon` on Legends articles in 4 of ~5 rows checked closely, and its `in repo` column mapped a repo sprite to the wrong canon droid entirely (T-series tactical sold as T1-series utility). When entry-writing contradicts an index, suspect the index (BENCH 2026-09-15)
- Never restart a review sidecar after handing the owner its URL: the port is part of the browser origin, so each restart orphans his tab and strands his verdicts in that origin's localStorage (2026-09-15, cost 9 rulings nearly lost; recovery is re-binding the exact port with --no-token, since `if not Ctx.token: return True`).
- review-sheets: the template ships `<script id="RENDER">` INSIDE an HTML comment, so a row renderer written there is inert text no scripted check can see — inject a live `<script>` instead (2026-09-15, cost: only the east thumb loaded on a three-facing art sheet).
- A pre-fill guard must key on savedBy/writeCount/savedAt; `touchedBySheet` is DERIVED by serve_sheet.py for --status and never written to disk, so guarding on it silently never fires (2026-09-15).
- Pre-filling an ART sheet on provenance (did the painterly wave touch it) mispredicted 6 of the owner's first 9 verdicts: he judges whether it LOOKS painterly, canon anatomy, and cross-facing scale. Facing HEIGHT consistency is measurable (height cannot change with viewing angle) and predicted his one flagged row at 1.85x — measure it before asking him to look (2026-09-15).
- The owner's in-game "call those done" art verdicts are not durable: Bolotaur and Gualaar were both reversed to rerender once seen on a sheet (2026-09-15).
- An alpha bounding box at `alpha > 0` lies on our art: a sub-visible export halo (alpha 1-16, <6% opacity) inflates it far past the painted body — FurnaceBeast_east has 3901 such pixels reaching y=480 while the body stops at y=400. Measure `alpha > 16` on the ORIGINAL, never a thumbnail (2026-09-15, produced two false "clean" verdicts already given to the owner).
- `point(lambda v: 1 if ...)` followed by `.convert("1")` silently zeroes everything, because convert("1") thresholds at 128 — it returned IoU 0.00 on all 40 sets, and only the impossible uniformity gave it away. Use 255 (2026-09-15).
- Comparing two sprites by cropping each to its own bbox and rescaling both to a square ERASES scale, so a wide profile and a narrow rear view look alike and a correct pair scored 0.86; comparing whole canvases instead matches on shared transparency and scored 0.83-0.98 for everything. Compare at native scale over the union silhouette (2026-09-15).
- The review-sheets template ships `<script id="RENDER">` inside an HTML comment because the row renderer is optional — code written there is inert text that no scripted check can see, and the sheet renders "fine" with only the first image per row loading. Inject a live `<script>` (2026-09-15).
- A background agent can misreport whose judgement a number is: mine returned creatures as the owner's `keep` verdicts when they were my own pre-fill and he had not ruled them. Verify attribution before repeating a subagent's number (2026-09-15).
- VALIDATION 2026-09-15 BENCH: a state assertion and the screen can both be right and disagree. The pit mod's `expect_pawn_despawned` PASSES — the pawn really leaves the map — while `Building_OpenPit` draws its first occupant via `IThingHolderWithDrawnPawn` over a vanilla 64px `TrapSpikeArmed` placeholder, so the player sees someone standing in a box labelled Pit. Any suite that asserts only state will certify any mod whose state is right and whose experience is absent. Recorded as `design/RimMandrake/north_star_validation_spec.md`.
- VALIDATION 2026-09-15 BENCH: a geometric image check can pass an asset the eye rejects, and no threshold fixes it. `Gizka_north.png` scores 0.845 mirror-symmetry (passes the 0.80 north/south floor in `art_checks.facing_symmetry`) while being an unmistakable side profile facing right — confirmed by looking. `facing_symmetry`'s own docstring concedes it cannot catch a face in the north, because a frontal face is symmetric too and a blobby profile is symmetric by accident. A visual judge caught it in 17.8s. Use this case as the counterexample whenever someone argues a numeric gate makes visual judgement unnecessary.
- modcheck: a checklist section's `### cannot show` ids are the OPPOSITE polarity; collecting every checkbox id in a section into one list put his rejection line into the must-show floor (found 2026-09-16, NORTH_STAR_RUNNER_WIRING_1)
- Never brief a subagent to "use RimSage" without checking it answers on THIS machine: it has never connected from the Laptop (five timeout logs, hosted service HTTP 000), and 3 of 5 fan-out agents burned full runs discovering that on 2026-09-16. Two returned honest UNMEASURED; the cost was ~15 min of parallel work.
- A status display whose bar and digits come from two different fields will eventually disagree and the disagreement will favour the wrong direction: statusline drew the bar from `used_percentage` and the number from `total_input_tokens` (the last request's UNCACHED input, ~7k against a 450k context), overstating context headroom by ~440k. Derive one from the other, never both from separate sources (2026-09-16).
- A doc can describe defects that were fixed before the doc was written: liquids_framework_design.md (2026-09-13) blocked all engine work on three FluidCanals flood defects fixed 2026-09-02 and closed at 747b0025. Check the code and the ledger before believing a doc's "engine status" section, and check whether an OPEN item is telling someone to re-fix fixed code.
- A gate cited by name outlives the item it names: NAMING_SCHEME_EXECUTION_1 closed 2026-08-31 yet ~20 live docs still said 'do not rename ahead of it' 16 days later, which is why FlowWorks still shipped as fluidcanals. Check an item's STATE before believing a doc that defers work to it.
- The north-star validated-hash covers the whole section, so editing surrounding PROSE un-validates a checklist by hash mismatch (hit live 2026-09-16 on Graffiti: deleting a now-false 'binds nothing until validated' caveat reverted VALIDATED to DRAFT). A freeze should cover the decisions, not the commentary around them, or documentation hygiene and validity are in direct conflict.
- Shared-worktree agents must commit with 'git commit -- <paths>' (pathspec form): three agents on 2026-09-16 ran git add + bare commit and swept other windows' staged files into their commit (two caught pre-push, one pushed and needed soft-reset recovery); 'never -A' in the brief does not prevent it — the pathspec form does.
- Cross-facing size audit (major-axis bbox spread, flag >15%) must run at WIRING time for every creature facing set — the sprite validator's structural gates do not check relative size across facings, and that gap is exactly the anooba-looks-huge defect class (2026-09-16 audit: all 12 current sets pass; nuna-female 14.3% and sytheclaw 10.6% are the borderliners).
- east/west sprite facings drift top-down even when the prompt says 'strict side profile'; the daemon facing hook must forbid the overhead camera explicitly (owner ruling 2026-09-16, gizka east) — and a canon entry's own hedge ('reads almost one-eyed') becomes a literal cyclops in the render.
- A north-star walk whose lines are in the wrong format parses as ZERO lines and still exits 0: `modcheck validate` prints "these lines BIND once validated" followed by nothing, so a walk validated in that state would bind an empty checklist and green the mod against nothing. The parser wants `- [ ] \`snake_case_id\` — text` under bold subheads; copy the format off a VALIDATED walk (Pits) rather than writing prose lines.
- A zero from an uncalibrated query is not a measurement: counting light fixtures in a ShipLayoutDefV2 export by looking for `<def>` elements returned 0 because the schema uses `<defName>`. The real answer was 11 of 2002 things. Discover the tag vocabulary first, then count — and treat any zero that would be convenient as a query bug until proven otherwise.
- `grep -c` counts matching LINES, not matches: it reported 48 mods in ModsConfig where XML parsing found 631. Any count off an XML file goes through a parser.
- `RUT_HumLayers.xml` asserts "no audio pipeline exists anywhere in this repo (confirmed by search)" — measured FALSE 2026-09-16: 1048 custom audio files in src/, including six ship-ambience sustainer defs and metal-creaking loops under Armoury's absorbed KotOR content. A prior agent's search result written into a comment ages into a false fact that blocks work.
- rimflow's `--owner-said` guard correctly refuses a quote containing a QUESTION as authorization ("the owner asking about a thing is not the owner authorizing it"). Split his message: quote only the imperative clause.
- handoff.py and rimflow cannot identify the seat on the Mac laptop (no seat profile, CLAUDE_CODE_SESSION_ID unset, and set_agent_window.sh only does the window half), so every ledger call there needs an explicit `--seat`. A design sitting's whole ledger trail lands mis-seated otherwise.
- A doc's "state" column decays FASTEST in the direction that causes rebuilding. FlowWorks' mechanics table said UNBUILT for 8 rows whose code exists (stock, limitless detection, partial fill, channel confinement, differentiated pathCost) because the code landed within a day of the table being written. Reading a checklist line back to the owner is what surfaced it — walking a checklist audits the design doc, not just the checklist.
- Reading a drafted checklist to the owner line by line produced three rulings that DELETED art, including one that dissolved a doc's self-described hardest, precedent-less asset into an asset already on the list. A line he cannot accept is usually a design claim nobody tested against his intent.
- A subagent's file:line evidence can carry a wrong PATH while its finding is right (cited Defs/TerrainDefs/ for a file at Defs/Canals/TerrainDefs/). Spot-verify the claims you intend to act on; the verdict and the citation fail independently.
- A walk file filed under a mod's old name is invisible to the tool that validates it: `find_walk("FlowWorks")` returned None while `FluidCanals.md` sat on disk, so yesterday's walked checklist could not have been validated at all. Check `find_walk` resolves before spending a sitting on a checklist. (2026-09-17, BENCH)
- A north-star bar about CHANGE was structurally unjudgeable and nothing said so: `judge.py` took `shots[-1]` under a prompt telling the model to answer about that image alone, while 10 of AtmosphericBase's 23 bars were about motion. Count how many of a checklist's bars the judge can actually see before drafting more of them. (2026-09-17, BENCH)
- A texture glob matching `*south*` catches BOTH `X_south.png` (base art) and `X_southm.png` (colour mask); masks are saturated across ~99% of pixels by convention, so `hits[0]` in filesystem order silently decides whether a head "has baked colour" — it inverted 4 of 7 decisions before I caught it via male/female Cathar reporting 253 vs 0. Measure `_south.png` alone. (2026-09-17, BENCH)
- `[ -e "$dir" ]` is not a test for "a mod lives here": `src/RimMandrake/Pits` passes while holding only `__pycache__`, and my checker reported 0 failures while missing a VALIDATED north star bound to an empty folder. Test `$dir/About/About.xml`. (2026-09-17, BENCH)
- A checker that reads a header from a FIXED line number lies quietly — `subject:` sits on line 2 in 77 walks and line 3 in AtmosphericBase.md, so a line-2 check passed by skipping the only broken file. Use the first matching line. (2026-09-17, BENCH)
- `modcheck run <Mod>` REWRITES the live ModsConfig.xml (via modlist_swap) despite reading like a query verb — it only failed on the Mac because the Windows path is absent; on the Desktop it swaps the list to MINIMAL unasked. Charter expensive-list action, unlabelled. (2026-09-17, BENCH)
- `shows=` appears in 0 of 17 mod `validation.py` files, so all 44 bars on the four VALIDATED mods are bound-and-uncovered and no `modcheck run` can green anything; `modcheck status` still prints GREEN for a mod that does not exist, because the summary reads a stored field while the detail re-derives. (2026-09-17, BENCH, measured twice independently)
- facing_set_audit now judges south camera elevation via claude -p (no pixel statistic separates top-down from front; measured S~N IoU/NCC overlap) — when a visual property resists numeric proxies, the ruled LLM transport IS the instrument; UNMEASURED must flag, never pass silent (BENCH 2026-09-17)
- 2026-09-17 (laptop): a backgrounded Agent is killed after **600 s with no streamed output** — "Agent stalled: no progress for 600s (stream watchdog did not recover)". Three died there today, all mid-read before their first write; the two that survived emitted output at 385 s and 575 s. So a long read-then-write brief is the shape that trips it: tell the subagent to **write a skeleton file immediately and fill it section by section**, because a file write emits progress and persists partial work. A stalled agent leaves NOTHING on disk — all three left a clean tree, so the whole run is lost, not merely truncated.
