# Biome paint list — source of truth for the ONE terminal repaint

**What this is.** A registry of every BiomeDef we own (`RM_*`, `RUT_*`, `RSW_*`), kept as
biomes finish, so the terminal painting pass (owner ruling 2026-09-20, see
`infrastructure/state/items/BIOME_PAINT_ONCE_AT_THE_END_1.md`) has a single source of
truth to paint from instead of re-deriving one under time pressure.

🔴 **A biome carrying 0 tiles below is the EXPECTED mid-migration state, not a defect.**
The planet is painted **once**, after every biome is its own mod (owner, 2026-09-20,
verbatim: *"Don't worry about worldmap painting. Once we have all the biomes in mods we
will do the painting once and for all."*). Nothing in this file gates, defers or escalates
any biome's build work on its tile count.

**Provenance of every number below:**
- **defs / build-state columns** — MEASURED live against the def dump, `mods=618/a48bc71544df1a7e captured=2026-09-20T20:14:27Z`, via `measure` (`~/.claude/skills/measuring-large-artifacts/bin/measure`), cross-checked by reading the mod XML on disk under `src/`. XML-derived facts are dated 2026-09-20 (today).
- **tile counts** — read from `world/ASHKARR_WORLDMAP_tiles.csv`, a RECORD exported from the savegame on **2026-09-12**. ⛔ Every tile count in this file is labelled "as of the 2026-09-12 export" and is NEVER a claim about the live planet right now — a live bridge edit after that date is invisible to this file, exactly as it was invisible to the CSV that produced `PYRELANDS_WRONG_BIOME_DEF_1`.
- **campaign region / target mod columns** — from the per-biome sheets in `design/Jawa/worldbuilding/biomes/` and the DRAFT proposal `design/RimMandrake/biome_mod_architecture.md` (owner-blocked, not yet ruled). Every name marked **PROPOSED** is that doc's invention, not an owner-ruled name — do not treat it as decided.

## Table

| defName | label | owning mod (folder / packageId) | active in ModsConfig (2026-09-20) | build state | campaign region(s), 2026-09-12 export | tiles, 2026-09-12 export | paint verdict |
|---|---|---|---|---|---|---|---|
| `RUT_Jawa_BackgroundWater` | mineral water | `UtinniPatches` / `mandrake.rut.patches` | yes | Deliberately minimal (44 lines): `implemented=false`, `isBackgroundBiome=true`, no terrain/plants/fauna/weather. Exists only so `WorldDrawLayer_Terrain.cs` has a background texture for TerrainMask landmarks — see its own header comment. | none — no tile can ever carry it by design | 0, by design | NO PAINT |
| `RUT_AridShrubland` | the arid shrubland | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (182 lines); FROZEN 2026-09-25 with one header comment (`LEANINGSCRUB_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.leaningscrub` from here on, this def stays byte-for-byte otherwise | Damp, Thornbelt, Grey Sea, Ashfall Range, Combs | 628 | NO PAINT — content merges into `RM_LeaningScrub`; frozen, carries the live world until Phase B, deleted then |
| `RUT_BlueDesert` | the Blue Desert | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (166 lines); FROZEN 2026-09-25 with one header comment (`BLUEDESERT_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.bluedesert` from here on, this def stays byte-for-byte otherwise | Deadstone, Sootreach, Cinderdark, Ammonia Flats, The Verge | 1029 | NO PAINT — content merges into `RM_BlueDesert`; frozen, carries the live world until Phase B, deleted then |
| `RUT_Contagion` | the Contagion (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (150 lines); FROZEN 2026-09-25 with one header comment (`CONTAGION_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.contagion` from here on, this def stays byte-for-byte otherwise | Dew Horn, Cratercrown, Ashfall Range, Dune Sea | 179 | NO PAINT — content merges into `RM_Contagion`; frozen, carries the live world until Phase B, deleted then |
| `RM_Contagion` | the Contagion | `src/RimMandrake/Contagion` / `mandrake.rm.contagion` | not yet — new this build (2026-09-25) | terrain + plants + fauna present — own `RM_BiomeWorker_Contagion` worldgen worker replacing donor `AlphaBiomes.BiomeWorker_OcularForest`; terrain/texture and all 10 `wildPlants` rows stay donor (Alpha Biomes) unguarded, inherited from the twin; 10 of 11 `wildAnimals` rows stay `MayRequire`'d on `sarg.alphaanimals`; the one remaining row, `RUT_Sytheclaw` (a RimMandrake-tier, not Star Wars, multi-homed creature still defined in RimUtinni), ships inline gated `MayRequire="mandrake.rut.patches"` rather than evicted (`BIOME_SPECIFIC_FAUNA_LAW_1` evictions are stopped); zero Star Wars rows exist in the roster at all, so unlike most sibling RM_ biome mods there is no `WildAnimals_Contagion.xml` | Dew Horn, Cratercrown, Ashfall Range, Dune Sea (from its twin `RUT_Contagion` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_Contagion` frozen 2026-09-25, merges in |
| `RUT_CrackedLands` | the Cracked Lands | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (162 lines); FROZEN 2026-09-21 with one header comment (`FLOODEDCANYON_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.floodedcanyon` from here on, this def stays byte-for-byte otherwise | Dew Horn, Cracklands, Damp, Dune Sea, Salt | 970 | NO PAINT — content merges into `RM_FloodedCanyon` (Q4 ruled 2026-09-21, verbatim "YES, same place"); frozen, carries the live world until Phase B, deleted then |
| `RUT_Desert` | the Desert | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (305 lines) | Long Sand, Thornbelt, Dry Marches, Combs, Sinkground | 2390 | PAINT |
| `RUT_ExtremeDesert` | the Extreme Desert | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 230 lines; FROZEN 2026-09-24 with one header comment (`STILLSAND_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.stillsand` from here on, this def stays byte-for-byte otherwise | Dune Sea, Glare, Kiln, Anvil, Long Sand | 3969 | NO PAINT — content merges into `RM_Stillsand`; frozen, carries the live world until Phase B, deleted then |
| `RUT_FeverWood` | the Fever Wood | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (232 lines) | Fever Wood | 43 | PAINT |
| `RUT_ForsakenCrags` | the Forsaken Crags | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (150 lines); FROZEN 2026-09-25 with one header comment (`FORSAKENCRAGS_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.forsakencrags` from here on, this def stays byte-for-byte otherwise | Gray Crags, Rimewall, Twilight Crags, Sunreach, Nightspill | 1135 | NO PAINT — content merges into `RM_ForsakenCrags`; frozen, carries the live world until Phase B, deleted then |
| `RUT_Greentide` | the Greentide (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 287 lines — campaign-specific: 22 Mlie Star Wars animals, 8 Star Wars plants, donor terrain `CypreJungleSoil` | Dune Sea, Cratercrown, Dew Belt, Anvil, Hollow Verge | 235 | NO PAINT — 🔴 **FROZEN 2026-09-22, and NOTHING MERGES OUT OF IT.** It carries the player's saved world until the terminal paint, then `RM_Greentide` takes over. ⛔ Copying this body into `RM_Greentide` would put Star Wars content inside a RimMandrake-tier def (§7 Q11 forbids it) — see `biome_mod_architecture.md` §4a and this def's own header. Twin decision owner-ruled via closed item `GREENTIDE_STANDALONE_MOD_1` |
| `RUT_GreySea` | the Grey Sea | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants (104 lines) — sea biome, impassable by design | Grey Sea | 472 | PAINT |
| `RUT_Miasma` | the Miasma (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (254 lines); FROZEN 2026-09-25 with one header comment (`MIASMA_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.miasma` from here on, this def stays byte-for-byte otherwise | Dune Sea, Fever Wood, Dew Horn, Salt Gate, Grey Sea | 93 | NO PAINT — content merges into `RM_Miasma`; frozen, carries the live world until Phase B, deleted then |
| `RM_Miasma` | the Miasma | `src/RimMandrake/Miasma` / `mandrake.rm.miasma` | not yet — new this build (2026-09-25) | terrain + plants + fauna present — own `RM_BiomeWorker_Miasma` worldgen worker replacing donor `AlphaBiomes.BiomeWorker_MiasmicMangrove`; six real mechanics (gradient axis, breath-tide surge, stranding pools, weather lock/exposure, fever-forged boons, warden/crèche placement) whose 16 def/patch files moved in with defNames unchanged, C# stays in the shared `mandrake.rm.environmentalhazards`; 11 wildAnimals rows inline (2 this campaign's own invented species still gated `MayRequire="mandrake.rut.patches"`, 9 Alpha Animals/VFE Insectoids donor rows); wildPlants stays donor (Alpha Biomes mangal family, 96.5% of flora weight) now `MayRequire`-gated where the twin left it unguarded; the 17 genuine Star Wars `wildAnimals` rows ride `WildAnimals_Miasma.xml` instead, and the 5 roster-evicted rows are dropped from both mods | Dune Sea, Fever Wood, Dew Horn, Salt Gate, Grey Sea (from its twin `RUT_Miasma` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_Miasma` frozen 2026-09-25, merges in |
| `RUT_NightsideIce` | the Nightside Ice | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants by design (185 lines) — DRAFT doc: "thin (no plants by design) but it is a Lantern Deeps host surface" | Deadstone, Sunreach, Gray Crags, Nightspill, Sootreach | 1506 | PAINT |
| `RUT_PoisonForest` | the Poison Forest (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 172 lines; FROZEN 2026-09-25 with one header comment (`POISONFOREST_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.poisonforest` from here on, this def stays byte-for-byte otherwise | Dew Belt, Grinding Floor, Twilight Sea, Slough, Sunreach | 546 | NO PAINT — content merges into `RM_PoisonForest`; frozen, carries the live world until Phase B, deleted then |
| `RM_PoisonForest` | the Poison Forest | `src/RimMandrake/PoisonForest` / `mandrake.rm.poisonforest` | not yet — new this build (2026-09-25) | terrain + plants + fauna present — own `RM_BiomeWorker_PoisonForest`, own `RM_PoisonSoil`/`RM_PoisonSoilRich` terrains replacing donor `PoisonSoil*` (undeclared BiomesPlus dependency in the twin), `RM_TwistingThornwood`/`RM_TreeMartyr`/`RM_DarkCrust` duplicated from the twin's flora (§7 Q13 — `RM_TwistingThornwood`'s species stays shared with `RUT_CrackedLands` via the untouched original), 13 non-Star-Wars fauna rows inline; the 5 Star Wars/campaign-tier rows (Neebray, Mynock, GraniteSlug, `RSW_Screecher`, `RSW_VentStalker`) ride `WildAnimals_PoisonForest.xml` as `RSW_`/donor casts instead, per Q11 | Dew Belt, Grinding Floor, Twilight Sea, Slough, Sunreach (from its twin's tiles) | 0 under this defName — mid-migration, not enabled on the live world yet | PAINT — target for the terminal repaint; `RUT_PoisonForest` merges in and is frozen |
| `RUT_PropaneLake` | the Propane Lake | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants (158 lines) | Umbra, Ammonia Flats | 57 | PAINT |
| `RUT_RustCathedral` | the Rust Cathedral | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (113 lines) | Rust Cathedral | 236 | PAINT |
| `RUT_Scarlands` | Warscar (owner ruling 2026-09-21, was "the Scarlands") | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (183 lines); ⚠️ its `workerClass` is vanilla `RimWorld.BiomeWorker_Scarlands` (label collision with a vanilla 1.6 biome) — inert either way, no worldgen on this world | Scorch | 90 | PAINT |
| `RUT_Slime` | the Slime (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 110 lines — thinner than its twin | Slough, Glass Reach, Nightspill, Chalk Marches | 96 | NO PAINT — **RULED 2026-09-21** (owner decision, taken by question card: he chose `RM_GelatinousSlime` as the surviving twin; not a verbatim quote): content merges into `RM_GelatinousSlime` per §4b; `RUT_Slime` is frozen, carries the live world until Phase B, deleted then |
| `RUT_Sump` | the Sump | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (140 lines) | Nightspill, Glass Reach, Damp, Scour, Twilight Crags | 41 | PAINT |
| `RUT_TheForge` | the Forge (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (176 lines); FROZEN 2026-09-25 with one header comment (`THEFORGE_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.theforge` from here on, this def stays byte-for-byte otherwise | Dune Sea, Anvil | 44 | NO PAINT — content merges into `RM_TheForge`; frozen, carries the live world until Phase B, deleted then |
| `RM_TheForge` | the Forge | `src/RimMandrake/TheForge` / `mandrake.rm.theforge` | not yet — new this build (2026-09-25) | terrain + plants + fauna present — own `RM_BiomeWorker_TheForge`, vanilla `World/Biomes/ExtremeDesert` texture and `RawAgave` foragedFood replacing the twin's Alpha Biomes donor texture/food, own `RM_FireLavender`/`RM_HeatsinkFungus` (straight renames) and `RM_CinderCrust` (§7 Q13 distinct-variant duplicate of the `RUT_Sagecrust` shared with `RUT_TheRot`, already claimed as `RM_Sagecrust` there), F1 weather-pulse cycle authored natively (`RM_ForgeStill`/`RM_BoilingRain`/`RM_ForgePulse`) plus an owned `RM_RedFog` replacing the twin's `RSW_SW_RedFog` (owner ruling 2026-09-24, drop the SW label), 4 non-Star-Wars donor fauna rows + the from-scratch `RM_FleetFlier` inline; the 3 Star Wars rows (`RSW_LavaFlea`, `RSW_Beldon`, `RSW_Maguana`) ride `WildAnimals_TheForge.xml` instead, casting the owned ports rather than the twin's bare donor `LavaFlea`/`Beldon` — fixing a latent mismatch where FORGE_MECHANICS_1's tibanna-tap patch has always targeted `RSW_Beldon`, a defName the twin never actually spawned. F2 tibanna gas/gather, F3 ThinkTree wiring, F4 foundry tower dungeon, F5 Contagion die-off ring and F6 vent buildings (`RUT_VentSmelter`/`VentForge`/`VentKiln`) are campaign plot or freestanding production content left to `FORGE_MECHANICS_1`'s own remaining pass, not moved here | Dune Sea, Anvil (from its twin's tiles) | 0 under this defName — mid-migration, not enabled on the live world yet | PAINT — target for the terminal repaint; `RUT_TheForge` merges in and is frozen |
| `RUT_TheRot` | the Rot (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 242 lines; FROZEN 2026-09-24 with one header comment (`THEROT_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.therot` from here on, this def stays byte-for-byte otherwise; carries 20 inline `AA_`/`RSW_`/`Snoruuk` fauna rows, campaign-specific | Nightspill, Frostcaps, Sporefields, Sootreach, Hanging Wood | 2204 | NO PAINT — content merges into `RM_TheRot`; frozen, carries the live world until Phase B, deleted then |
| `RUT_TheScald` | the Scald | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants (178 lines) — sea biome | Scald | 312 | PAINT |
| `RUT_TwilightSea` | the Twilight Sea | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants (132 lines) — sea biome | Twilight Sea | 607 | PAINT |
| `RUT_FuelSnows` | the Fuel Snows | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (renamed 2026-09-21 from `RUT_Umbra`, content unchanged, `UMBRA_IS_A_REGION_NOT_A_BIOME_1` — "Umbra" now names the region, not this biome) | Deadstone, Umbra, Ammonia Flats, Fuelmere, Lantern Deeps | 0 — RE-MEASURED 2026-09-21 live against the canonical save's own tile array (`worldmap.py`, not the CSV): 0 tiles carry this defName's shortHash. All 2,531 live tiles are still tagged `RUT_Umbra` (see that row) | PAINT — the forward-authored def; target for the terminal repaint |
| `RUT_Umbra` | Umbra (compat duplicate) | `UtinniPatches` / `mandrake.rut.patches` | yes — RESTORED 2026-09-21 | full duplicate of `RUT_FuelSnows`'s content, kept alive only so this defName resolves | Deadstone, Umbra, Ammonia Flats, Fuelmere, Lantern Deeps | 2531 — RE-MEASURED 2026-09-21 (`worldmap.py` decode of `CANONICAL_ASHKARR_START_2026-09-12.rws`'s live `tileBiome` array against a pre-deletion def-dump capture, shortHash 15270): **CONFIRMED, not stale** — the 2026-09-12 export's 2531 figure still holds because nothing has repainted these tiles since. `UMBRA_IS_A_REGION_NOT_A_BIOME_1`'s rename had deleted this defName outright and left these 2531 tiles unresolvable; restored as a compat duplicate pending the terminal repaint. | NO PAINT (temporary) — retire once the terminal repaint moves these 2531 tiles onto `RUT_FuelSnows`; verify with the same live decode before deleting |
| `RUT_Wasteland` | the Wasteland | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + fauna present, no plants (196 lines); FROZEN 2026-09-24 with one header comment (`WASTELAND_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.wasteland` from here on, this def stays byte-for-byte otherwise | Sunreach, Ashen Wastes, Scour, Salt, Blight | 1853 | NO PAINT — content merges into `RM_Wasteland`; frozen, carries the live world until Phase B, deleted then |
| `RUT_Webwork` | the Webwork | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present (160 lines) | Dune Sea, Cratercrown, Hollow Verge, Dew Belt, Anvil | 161 | PAINT |
| `RUT_WeepingStones` | the Weeping Stones (campaign twin) | `UtinniPatches` / `mandrake.rut.patches` | yes | terrain + plants + fauna present, 203 lines; FROZEN 2026-09-24 with one header comment (`WEEPINGSTONES_RM_MOD_BUILD_1`) — content lives in `mandrake.rm.weepingstones` from here on, this def stays byte-for-byte otherwise; carries 8 inline Star Wars `mlie.starwarsanimalcollection` fauna rows, campaign-specific | Dew Belt, Dew Horn, Dune Sea, Cratercrown, Anvil | 223 | NO PAINT — content merges into `RM_WeepingStones`; frozen, carries the live world until Phase B, deleted then |
| `RM_WeepingStones` | the Weeping Stones | `src/RimMandrake/WeepingStones` / `mandrake.rm.weepingstones` | yes | terrain + plants + fauna present — franchise-free body only (`ColossusToad`, `AA_Eyeling`, `Plant_Reeds`, `AB_GreenRockFern`, `RUT_Dewshrooms`, `Plant_Ambrosia`); the 8 Star Wars fauna ride `WildAnimals_WeepingStones.xml` as `RSW_` casts instead, per Q11. Own `RM_BiomeWorker_WeepingStones`, own duplicated fish bestiary/rare-catches. Owed: the `WEEPING_STONES_DESIGN_SITTING_1` (2026-09-24) ruled invented cast (8 fauna, flora additions incl. `RM_Shadefern` replacing `AB_GreenRockFern`) — RULED but not yet authored as ThingDefs | Dew Belt, Dew Horn, Dune Sea, Cratercrown, Anvil (from its twin's tiles) | 0 under this defName — mid-migration, not enabled on the live world yet | PAINT — target for the terminal repaint; `RUT_WeepingStones` merges in and is frozen |
| `RM_LanternDeeps` | lantern deeps | `src/RimMandrake/LanternDeeps` / `mandrake.rm.lanterndeeps` | yes | terrain + plants present, own pocket-map generator; RENAMED 2026-09-25 (`LANTERNDEEPS_RM_MOD_BUILD_1` — an injection layer, no `RUT_` twin to freeze, so this row is a straight rename in place, not a twin split); the 8 Star Wars fauna rows moved to `WildAnimals_LanternDeeps.xml` (UtinniPatches); the FROZEN sheet `the_lantern_deeps.md` states it explicitly: "Not a worldmap biome (owner: 'that was a mistake') — an injected underground layer beneath any nightside map" | none by design — no surface tile carries this def; it is injected under nightside maps below −40 °C | 0, by design | NO PAINT |
| `RM_TheRot` | the Rot | `src/RimMandrake/TheRot` / `mandrake.rm.therot` | not yet — new this build | terrain + plants + fauna present — absorbs the former `mandrake.rut.rotsporekit` kit (151 files, `RUT_`->`RM_` renamed) whole: own `RM_BiomeWorker_TheRot`, own `RM_TheRotGrass`/`RM_TheRotSoilRich` terrains replacing donor `AB_Mycotic*`, all 30 wildPlants rows (17 renamed kit flora + 13 `AB_` donor, Q9) plus 5 native guardian-grove/pale-tree rows, wildAnimals holds only the 8 `sarg.alphaanimals` donor rows. The 11 `RSW_` fauna rows + Snoruuk ride `WildAnimals_TheRot.xml` as Utinni-patch casts instead, per Q12/Q11a. Own Mod Settings screen; step 5 (load-proof) owed on the Desktop | Nightspill, Frostcaps, Sporefields, Sootreach, Hanging Wood (from its twin's tiles) | 0 under this defName — mid-migration, not enabled on the live world yet | PAINT — target for the terminal repaint; `RUT_TheRot` merges in and is frozen |
| `RM_Pyrelands` | the Pyrelands | `src/RimMandrake/Pyrelands` / `mandrake.rm.pyrelands` | yes | terrain + plants + fauna present, 322 lines — the richest def in the whole registry | Pyrelands (per the region column) | 0 under this defName in the 2026-09-12 export — that day the 63 "Pyrelands"-region tiles were recorded under the **donor** `ZBiome_Grasslands`, not this def. A committed comment from a LIVE bridge read on 2026-09-19 asserts the reverse. Which def the tiles carry **right now** is UNMEASURED by this pass — deliberately, per `BIOME_PAINT_ONCE_AT_THE_END_1`: the terminal paint pass reads the live world once and settles it, this file does not. | PAINT — this is the only Ash'karr-owned def for the Pyrelands; `ZBiome_Grasslands` is a *More Vanilla Biomes* donor def, not ours, and is out of scope for this registry |
| `RM_Greentide` | the Greentide | `src/RimMandrake/Greentide` / `mandrake.rm.greentide` | yes | ✅ **the finished generic twin, complete — not a placeholder.** 123 lines because it is deliberately campaign-free: 7-animal vanilla roster, 6-plant vanilla list, vanilla `Soil`/`SoilRich`, vanilla `TropicalRainforest` texture, `mudTerrain` `RM_GreentideChurnmud`, own `RM_GreentideBiomeRanges` extension. Q11-compliant already. Star Wars fauna reach it by patch (`WildAnimals_Greentide.xml`), never in the def | Dune Sea, Cratercrown, Dew Belt, Anvil, Hollow Verge (from its twin's 2026-09-12 tiles) | 0 under this defName in the 2026-09-12 export — all 235 tiles that day were recorded under the twin `RUT_Greentide` | PAINT — ruled survivor of the twin pair, closed item `GREENTIDE_STANDALONE_MOD_1` |
| `RM_FloodedCanyon` | flooded canyon | `src/RimMandrake/FloodedCanyon` / `mandrake.rm.floodedcanyon` | **NOT active** in the campaign's full modlist — a def carrying 0 tiles by design is the expected mid-migration state (`BIOME_PAINT_ONCE_AT_THE_END_1`), not a defect | terrain + plants + fauna present, own generic vanilla-Core body plus 3 ported disease entries (`FLOODEDCANYON_RM_MOD_BUILD_1`); the Star Wars fauna/flora, the campaign weather ban and the "soil only from the flood" terrain override ride `src/RimUtinni/UtinniPatches/Patches/WildAnimals_CrackedLands.xml` as PatchOperationReplace/Add onto this def, not this def's own body — RM_'s generic roster stays intact for a non-campaign world | Cracked Lands | 0 — mod not enabled in the full campaign modlist yet; carries 0 tiles by design until Phase B | PAINT — ruled survivor of the twin pair (Q4, 2026-09-21, "same place"); RUT_CrackedLands merges in and is frozen |
| `RM_LeaningScrub` | the leaning scrub | `src/RimMandrake/LeaningScrub` / `mandrake.rm.leaningscrub` | not yet — new this build (2026-09-25) | terrain + plants present, own generic body: `RM_Fuzz`/`RM_Grellbush`/`RM_Grellspine`/`RM_WildHealroot`/`RM_SweetlineTree` (renamed from the frozen twin's `RUT_` plant defs, §7 Q8) plus 5 non-Star-Wars donor fauna rows (Terrorworm, 4 `AA_` rows, Q9) and the shared `RM_VenomvineThicket`; the 40 Star Wars fauna rows ride `WildAnimals_LeaningScrub.xml` as `PatchOperationAdd` instead, per Q11. `RM_SweetlineTree` ships but is deliberately not wired into `wildPlants` — neither is its frozen twin — pending `TREE_GRAPHICS_OWNERSHIP_1`'s placement ruling | the leaning scrub (from its twin's tiles) | 0 under this defName — mid-migration, not enabled on the live world yet | PAINT — target for the terminal repaint; `RUT_AridShrubland` merges in and is frozen |
| `RM_GelatinousSlime` | the gelatinous slime | `src/RimMandrake/GelatinousSlime` / `mandrake.rm.gelatinousslime` | **ACTIVE — CORRECTED 2026-09-21.** Confirmed present in `infrastructure/state/modlists/ModsConfig.FULL.LATEST.xml` (620 active mods, parsed via `xml.etree.ElementTree`, not grep). The "NOT active" claim below was true against the 2026-09-20T20:14:27Z capture (618 mods) but is stale as of `SLIME_GENE_ARCHIVE_BUILD_1`'s close (`f8304235e`), whose own prose recorded the mod as live during its quicktest and the doctrinal full list as having been missing it before recapture. | terrain + plants + fauna present, 198 lines, richest of the Slime twin pair; as of today also ships the full 33-target A/B-list gene/kit system (`SLIME_GENE_ARCHIVE_BUILD_1`, live-verified) | Slime (proposed pairing, per its twin's tiles) | 0 under this defName in the 2026-09-12 export (mod was inactive then) — tile count is UNMEASURED now that the mod is active; not remeasured here per `BIOME_PAINT_ONCE_AT_THE_END_1` | PAINT — **ruled survivor of the twin pair, 2026-09-21** (owner decision, taken by question card, not a verbatim quote); `RUT_Slime` merges in and is frozen |
| `RM_TheScald` | the Scald | `src/RimMandrake/TerminalBiomes` / `mandrake.rm.terminalbiomes` | not yet — new this build (2026-09-25) | terrain (own `RUT_ScaldWater`/`RUT_ScaldMargin` moved in) + full Scald kit + 8-species catch; floor cast noohm/shulla RULED at the 2026-09-25 sitting, defs owed; the 4 `RSW_` floor animals ride `WildAnimals_TheScald.xml` (Q12) | the Scald (from its twin's tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_TheScald` frozen 2026-09-25, merges in |
| `RM_PropaneLake` | the Propane Lake | `src/RimMandrake/TerminalBiomes` / `mandrake.rm.terminalbiomes` | not yet — new this build (2026-09-25) | own `RM_PropaneLakeDeep`/`RM_SolidPropane` terrains replacing donor `AB_*`; cast vaunoom/heemin/oovanam/hoolen RULED 2026-09-25 (fishable — heemin/oovanam catch), defs owed; NO shelf biome (owner, typed, 2026-09-25: word confusion, no change) | the Propane Lake (from its twin's tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_PropaneLake` frozen 2026-09-25, merges in |
| `RM_TwilightSea` | the Twilight Sea | `src/RimMandrake/TerminalBiomes` / `mandrake.rm.terminalbiomes` | not yet — new this build (2026-09-25) | 10-species catch folds in (fish retier RUT_→RM_ RULED 2026-09-25); floor cast noolim/loohn/weloon/lunoowa RULED, defs owed; `RSW_` fauna rides `WildAnimals_TwilightSea.xml` (Q12) | the Twilight Sea (from its twin's tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_TwilightSea` frozen 2026-09-25, merges in |
| `RM_GreySea` | the Grey Sea | `src/RimMandrake/TerminalBiomes` / `mandrake.rm.terminalbiomes` | not yet — new this build (2026-09-25) | sparse by law; floor cast fessk/sorruth/essarn/otheska RULED 2026-09-25, catch = brine chemistry-life (essarn/sorruth — newer sea law supersedes the Grey's no-fish ruling), defs owed; `RSW_` fauna rides `WildAnimals_GreySea.xml` (Q12) | the Grey Sea (from its twin's tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_GreySea` frozen 2026-09-25, merges in |
| `RM_NightsideIce` | the Nightside Ice | `src/RimMandrake/NightsideIce` / `mandrake.rm.nightsideice` | not yet — new this build (2026-09-25) | plantless ice biome; 7 `AA_*` donor rows kept in the def, `RSW_CaveLemming` rides `WildAnimals_NightsideIce.xml` (Q12); vanilla `BiomeWorker_IceSheet` kept deliberately | Nightside (from its twin's tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_NightsideIce` frozen 2026-09-25, merges in |
| `RM_Stillsand` | the Stillsand (deep desert) | `src/RimMandrake/Stillsand` / `mandrake.rm.stillsand` | not yet — new this build (2026-09-25) | vanilla `BiomeWorker_ExtremeDesert` reused deliberately (item finding); all `RSW_`/`JOE_` rows ride `WildAnimals_Stillsand.xml` (Q12/Q11a) | deep desert (from its twin `RUT_ExtremeDesert` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_ExtremeDesert` frozen 2026-09-25, merges in |
| `RM_LongShade` | the Long Shade (desert) | `src/RimMandrake/LongShade` / `mandrake.rm.longshade` | not yet — new this build (2026-09-25) | vorrel line moved in from AshkarrFlora (RUT_Staggerseed* → RM_Vorrel*); 51 `RSW_` rows ride `WildAnimals_LongShade.xml` (Q12) | desert (from its twin `RUT_Desert` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_Desert` frozen 2026-09-25, merges in |
| `RM_Wasteland` | the Wasteland | `src/RimMandrake/Wasteland` / `mandrake.rm.wasteland` | not yet — new this build (2026-09-25) | brine terrains/deposits/items + flora + own worker; `RSW_` rows ride `WildAnimals_Wasteland.xml` (Q12); ⚠️ terrainsByFertility copies donor `VolcanoSoil`/`WastelandAsphalt` unguarded — robustness-pass item | wasteland (from its twin `RUT_Wasteland` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_Wasteland` frozen 2026-09-25, merges in |
| `RM_ForsakenCrags` | the Forsaken Crags | `src/RimMandrake/ForsakenCrags` / `mandrake.rm.forsakencrags` | not yet — new this build (2026-09-25) | own `RM_BiomeWorker_ForsakenCrags` worldgen worker replacing donor `AlphaBiomes.BiomeWorker_RockyCrags`; terrain/texture/8 wildPlants stay donor (Alpha Biomes/Alpha Genetics) unguarded, inherited from the twin (§5 step 2 "copy verbatim", not this build's scope to fix — disclosed in About.xml); 14 wildAnimals rows stay `MayRequire`'d on `sarg.alphaanimals`; `RSW_Cindermare`/`RSW_Skarnix` ride `ForsakenCrags_WildSpawns.xml` (Q12/Q11a) — retargeted this build from the donor `AB_RockyCrags` (its original, never-fixed target) to this def | Gray Crags, Rimewall, Twilight Crags, Sunreach, Nightspill (from its twin `RUT_ForsakenCrags` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_ForsakenCrags` frozen 2026-09-25, merges in |
| `RM_BlueDesert` | the Blue Desert | `src/RimMandrake/BlueDesert` / `mandrake.rm.bluedesert` | not yet — new this build (2026-09-25) | vanilla `BiomeWorker_IceSheet` kept deliberately (twin never named a donor type); ships its full commissioned cast moved in unchanged — `RM_Dorrak`/`RM_Krissek`/`RM_Vekkit` fauna, `RM_Palefloss`/`RM_Glassfern`/`RM_Chimeglobe` flora, `RM_ColdWax`, the detonation-chain hediffs, the krissek halo effecter, and the mechanic C# (`BlueDesertLife.cs` + its own `RM_BlueDesertSettings`, moved verbatim off `UtinniPatchesSettings`); `AA_Thunderbeast` (Alpha Animals, non-Star-Wars) ships inline `MayRequire`'d; three `wildPlants` rows (`AB_ToxiGrass`/`AB_CrystalHorn`/`PoisonPlantTallGrass`) stay donor-unguarded, inherited from the twin; genuinely-canon `Vapaad` rides `WildAnimals_BlueDesert.xml` instead (Q11a) — correcting the twin's own comment, which had called both Star Wars rows | Deadstone, Sootreach, Cinderdark, Ammonia Flats, The Verge (from its twin `RUT_BlueDesert` tiles) | 0 under this defName — mid-migration | PAINT — target for the terminal repaint; `RUT_BlueDesert` frozen 2026-09-25, merges in |

Donor def out of scope, noted for context only: `ZBiome_Grasslands` (*More Vanilla
Biomes*) is not `RM_*`/`RUT_*`/`RSW_*` and is not ours — it does not get a row, but it
carried 222 Pyrelands-region tiles in the 2026-09-12 export (see the `RM_Pyrelands`
row above).

No `RSW_*` BiomeDefs were found in the 618-mod def dump or in a source search of `src/`
as of 2026-09-20 — Star Wars content is fauna/flora patched onto RimMandrake/Utinni
biome defs, not its own biome tier (matches `biome_mod_architecture.md` §1: "only Star
Wars creatures become patches").

## Counts

Recounted row by row from the table above, 2026-09-21, after the last twin ruling landed.
🔴 **STALE as of 2026-09-24** — `WEEPINGSTONES_RM_MOD_BUILD_1` split a fifth twin pair
(`RUT_WeepingStones` frozen / `RM_WeepingStones` new) the same day these totals were last
recomputed; the two bullets below have not been re-added up. Do not cite the numbers below
as current without re-deriving them from the table.

- **PAINT: 27** — the 23 standalone `RUT_` regions not otherwise flagged (including
  `RUT_FuelSnows`, renamed 2026-09-21 from `RUT_Umbra` and the forward target for the
  terminal repaint), plus the four ruled twin survivors `RM_Pyrelands`, `RM_Greentide`,
  `RM_FloodedCanyon` and `RM_GelatinousSlime` — now also `RM_WeepingStones` (2026-09-24,
  not recounted into this total).
- **NO PAINT: 6** — `RUT_Jawa_BackgroundWater` and `RM_LanternDeeps` (both never carry a
  tile by design; the latter renamed from `RUT_LanternDeeps` 2026-09-25, no twin involved);
  the three frozen twins whose content is owed to an `RM_` survivor,
  `RUT_Greentide`, `RUT_CrackedLands` and `RUT_Slime` — now also `RUT_WeepingStones`
  (2026-09-24, not recounted into this total); and `RUT_Umbra` (restored 2026-09-21
  as a temporary compat duplicate of `RUT_FuelSnows` — see its row; carries the live 2,531
  tiles until the terminal repaint moves them, then retires).
- **UNDECIDED: 0** — every twin question is ruled. Cracked Lands / Flooded Canyon on
  2026-09-21 (his words, "YES, same place"); Slime on 2026-09-21 (his decision taken by
  question card — `RM_GelatinousSlime` survives — so there is no verbatim quote for it);
  Weeping Stones is not a contested twin question at all — `WEEPINGSTONES_RM_MOD_BUILD_1`
  is the from-scratch Phase A shape (no prior `RM_` mod existed to choose between).
- **Total rows: 34** owned BiomeDefs (one more than the 33 last counted 2026-09-21 — this
  row's own `RM_WeepingStones` addition).

## Needs an owner decision

**Nothing.** As of 2026-09-21 every decision this registry was waiting on is ruled:

- **Cracked Lands vs Flooded Canyon** — ruled (his words, *"YES, same place"*).
  `RM_FloodedCanyon` survives; `RUT_CrackedLands` is frozen. `FLOODEDCANYON_RM_MOD_BUILD_1`
  is built, live-proven and closed.
- **Slime twin** — ruled by question card: `RM_GelatinousSlime` survives, `RUT_Slime` is
  frozen (`SLIME_STANDALONE_MOD_1`).
- **The architecture doc is no longer DRAFT.** `design/RimMandrake/biome_mod_architecture.md`
  carries all ten §7 rulings and every biome name is picked; its status line now reads RULED
  and Phase A is executing. Mod names in this file's build-state notes that trace to it are
  **decided**, not proposed.

⚠️ Still true and still not a defect: the real owning mod for most of these defs is the
single `mandrake.rut.patches` (UtinniPatches), because Phase A is mid-flight — 1 of 23
per-biome build items is closed. That is the expected mid-migration state.

## Unmeasured

- Whether `RM_FloodedCanyon`'s mod **folder** exists in the live Steam Mods directory (only
  its absence from `ModsConfig.xml` was checked here). `RM_GelatinousSlime`'s is confirmed
  active as of 2026-09-21 — see its row above — so this no longer applies to it.
- The **live**, right-now BiomeDef under the Pyrelands tiles (deliberately not measured by
  this pass — see the `RM_Pyrelands` row and `BIOME_PAINT_ONCE_AT_THE_END_1`).
- Full completeness (hediffs, game conditions, mod-settings gating) per biome beyond
  terrain/plants/fauna presence — this pass checked structural presence of those three
  blocks only, via `grep` on each biome's own small source XML file (not a large-artifact
  scan; the def dump and disk source agree on which files exist).

## Roster hole-filling policy — owner, 2026-09-22

When `BIOME_SPECIFIC_FAUNA_LAW_1` (one animal, one biome) withdraws a species from a roster,
it leaves a hole, and his law's other half says holes are filled with **new** creatures. He
scoped when that happens:

> *"We will fill rosters as per-biome work continues. Only fill the mostly finished biomes as
> needed: deep desert, desert, rot, pyrelands, lantern deeps. Others we will work
> individually."*

- ✅ **Fill now, as needed — the mostly-finished five:** the deep desert (`RUT_ExtremeDesert` /
  `RM_Stillsand`), the desert (`RUT_Desert` / `RM_LongShade`), the Rot, the Pyrelands, the
  Lantern Deeps.
- ⛔ **Do NOT file fill work for any other biome** on the strength of a hole alone — those are
  worked individually as their own per-biome pass reaches them. A withdrawn *admission* that
  was never wired changes nothing in game and is not a hole worth chasing.
