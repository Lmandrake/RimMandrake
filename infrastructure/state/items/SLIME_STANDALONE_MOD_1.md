# SLIME_STANDALONE_MOD_1 — the Slime twin is ruled: `RM_GelatinousSlime` survives

## the ruling

**Owner, 2026-09-21.** Asked which biome-mod survives the Slime twin pair, he chose
`RM_GelatinousSlime`. ⚠️ **Taken by question card, not in prose** — he selected the option,
so there is no verbatim sentence of his to quote for this one. Do not manufacture one.

This is the sibling of `GREENTIDE_STANDALONE_MOD_1` (closed) and of the Cracked
Lands / Flooded Canyon call (his words that day: *"YES, same place"*). All four twin
questions in `design/RimMandrake/biome_mod_architecture.md` are now ruled; none is open.

## what it settles

- **`RM_GelatinousSlime` (`mandrake.rm.gelatinousslime`) is the survivor** and the paint
  target. It was already the richer article — slime terrain ladder, compressor, gene
  archive, antidote, four plants, `SlimeMod.cs` settings — and as of the same day it also
  carries the full 33-target A/B-list gene system (`SLIME_GENE_ARCHIVE_BUILD_1`,
  live-verified), which widened the gap further.
- **`RUT_Slime` is the loser.** Its content merges in per §4b; it is then FROZEN with one
  header comment and carries the live world until the terminal repaint. ⛔ It is deleted at
  Phase B step 4 and **not before** — deleting a painted def early destroys the save.
- **The execution is not this item.** It is `GELATINOUSSLIME_RM_MOD_BUILD_1` (open, FOUNDRY),
  whose spec already presupposed this survivor; this item exists so the ruling is greppable
  the way its Greentide sibling is.

## spec

Record the ruling where the decision is read, then close. No build work belongs here.

1. `design/RimMandrake/biome_mod_architecture.md` §4b — survivor line marked RULED. ✅ done
2. `infrastructure/state/facts/biome_paint_list.md` — `RUT_Slime` row → NO PAINT,
   `RM_GelatinousSlime` row → PAINT, counts recomputed, the "needs an owner decision"
   entry removed rather than annotated. ✅ done

## verify

No file states the Slime twin is UNDECIDED or awaiting an owner call, and no file attributes
a verbatim quote to him for this ruling.

## criteria

A cold reader picking up `GELATINOUSSLIME_RM_MOD_BUILD_1` finds the survivor already
settled, with the provenance of the decision stated honestly.
