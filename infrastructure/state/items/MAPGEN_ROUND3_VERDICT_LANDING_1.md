# MAPGEN_ROUND3_VERDICT_LANDING_1 — the owner's round-3 grade, for FOUNDRY to absorb

Filed by BENCH from the 2026-09-09 evening sitting. The comparator sheet
(`Transient/mapgen_v3/comparator_sheet.png`) was graded live by the owner.
BENCH may not edit `MACRO_GENERATOR_V0_1.md` (seat ownership); this item
carries the verdict for FOUNDRY to fold in.

## The grade

**FAIL — 0 of 8 premises readable** (the item's own bar was ≥3). Verbatim:

> *"This is... extremely challenging to provide useful feedback to. They don't
> look anything like the provided maps, that's for sure. The canyon ones where
> you said 'the only way through' doesn't seem clear to map to any of those
> labels... they're all about the same."*

He could not grade map-by-map because the maps are not distinguishable
map-by-map.

## Three defects, translated

1. **Gestalt failure against corpus.** The generated maps are in-band on every
   measured statistic and still read as nothing beside the real maps — the
   item's own LIES row, now confirmed by the only instrument that counts.
   Stop tuning toward `corpus_stats.py`; it is measuring the wrong thing.
2. **Choke-point premises are invisible.** "The only way through is at the
   narrows/head/lip/ring centre" produced nothing the owner could find. A
   passage premise must read as a passage at thumbnail size or the premise
   is not on the map.
3. **Canyon variants are interchangeable.** Four canyon maps, two sharing one
   premise sentence, all reading identical — one-idea-per-map is failing at
   the differentiation step, not just the rendering step.

## Disposition (BENCH's sequencing, owner may veto)

Hold painter round 4. `MAPGEN_GL_SHEET_1` (the owner's 2026-09-06 "both
routes" order — real in-game quicktest screenshots beside painter renders) is
now the lead: it answers whether the gestalt gap is content or the offline
painter's rendering, which no amount of painter iteration can answer about
itself. One open question worth settling in that pass: whether the corpus
crops on the comparator were produced by the same renderer as the generated
maps — if not, the round-3 comparison was never like-for-like.

## criteria

- MACRO_GENERATOR_V0_1 carries this grade in its own file (FOUNDRY's pen) and
  its next round is designed against the three defects, not against corpus
  stats.
- Painter round 4 does not run before the GL/quicktest comparison exists.

## close

Close when the grade is absorbed into MACRO_GENERATOR_V0_1 and the GL-first
sequencing is either followed or explicitly overruled by the owner.
