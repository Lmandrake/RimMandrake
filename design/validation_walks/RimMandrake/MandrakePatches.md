# RimMandrake Patches — validation walk
subject: src/RimMandrake/MandrakePatches  (packageId `mandrake.rm.patches`)
deps: 10 third-party mods, each patch MayRequire/Conditional-guarded to that one patch only — `biomesteam.biomescaverns`, `mlie.starwarsanimalcollection`, `Who.VFEE.IsopodaGeneline` (AnimalDessicatedTexPaths_Fix); `btd.remix.gravshipblueprints` (BTDGravshipQuest_GrammarFix); `IronScruff.PrimordialGeysers` (BiomeAnimalDanglingRefs_Fix); `det.buzzers` (BuzzerApostrophe_Fix); `mlie.miningcodrillturret` (DrillTurret_ShootingJob); `GRimTerra.Biomesmod` (GrimTerraTexPaths_Fix); `Nals.FacialAnimation` + `DanZinagri.FacialAnimationCompatabilityProject` + `ab.hoffa` (HeadSetForFA_Revive — `ab.hoffa` is not currently in the active list, patch is a designed no-op until it is); `Dark.Signs` (ThirdPartySignConfigErrors_Fix)
list: full     # 9 patches each target a different third-party mod; only WorldMapReadability_Ashkarr (targets vanilla Settlement) runs on minimal
status-hint: game-generic fixes and compatibility patches, MayRequire/Conditional-guarded so a missing target is a silent no-op — 9 patch files, each fixing one third-party defect (dessicated tex paths, a grammar-breaking credit line, dangling biome animal refs, an apostrophe rule, a drill turret's work type, sign impassability config errors, a facial-animation revival, world-map icon size)

## must be true
- Every one of the 9 patch files is guarded by `PatchOperationFindMod`/`MayRequire`/`PatchOperationConditional` on its own third-party target — with that mod absent, the patch must produce ZERO log lines (a patch that matches nothing logs nothing; this must be verified as an ABSENCE of error, not confused with silent no-op success).
- `DrillTurret_ShootingJob.xml`: `WorkGiverDef` `OperateDrillTurret`'s `workType` becomes `Hunting` (deliberately, per the owner's ruling — not `Crafting`), when `MiningCo. DrillTurret (Continued)` is active.
- `ThirdPartySignConfigErrors_Fix.xml`: all 9 named `Dark.Signs` ThingDefs (`SignBillboard`, `SignCairn`, `SignNature`, `SignObelisk`, `SignPanel`, `SignPanelWood`, `SignRoadLED`, `SignSlab`, `SignWoodenMulti`) carry `disableImpassableShotOverConfigError=true` when `Dark.Signs` is active — the live Player.log's `Config error in <defName>` lines for these 9 must be GONE relative to the frozen baseline `infrastructure/state/facts/config_error_baseline_2026-09-03.txt`.
- `WorldMapReadability_Ashkarr.xml`: `WorldObjectDef` `Settlement`'s `expandingIconDrawSize` = `2` — this one runs unconditionally (Settlement is a Core def, always present), so it is the ONE check in this file that works on the minimal list.
- `BTDGravshipQuest_GrammarFix.xml`: the `[BTD] Gravship Blueprints Quest` credit line no longer appears inside `BTD_DownedGravship`'s `questDescriptionRules`, and the quest's rendered description is non-empty (was previously entirely discarded by an unresolvable `[BTD]` grammar symbol) — only when `btd.remix.gravshipblueprints` is active.

## the walk
1. [L] Player.log after load contains no "Config error in mandrake.rm.patches" itself, AND (per third-party mod actually active) no "Config error in Sign" naming any of the 9 sign defNames above   # load-time
2. [D] def read-back: `WorldObjectDef` `Settlement` `expandingIconDrawSize` = `2` — runs regardless of which third-party mods are active (minimal list sufficient)
3. [D] (only if `mlie.miningcodrillturret` active) def read-back: `WorkGiverDef` `OperateDrillTurret` `workType` = `Hunting`
4. [D] (only if `Dark.Signs` active) def read-back: each of the 9 Sign ThingDefs carries `disableImpassableShotOverConfigError` = `true`
5. [D] (only if `btd.remix.gravshipblueprints` active) def read-back: `QuestScriptDef` `BTD_DownedGravship`'s `questDescriptionRules/rulesStrings` contains no `li` with the literal substring `[BTD]`
6. [B] (only if `btd.remix.gravshipblueprints` active) jawa/incident_schedule or jawa/fire_incident to trigger `BTD_DownedGravship`, then jawa/letter_list / rimworld/list_letters → confirm the quest's rendered description text is non-empty in the Quests tab (not just the raw def field, since grammar resolution happens at RUNTIME)
7. [L] (only if `IronScruff.PrimordialGeysers` active) confirm no "Config error" or dangling-reference warning naming `IronScruff_PrimordialGeysers`'s `wildAnimals` list entries (`AA_WaywardMobileAssembler`, `AEXP_Badger`, `AEXP_Muskox`, etc.) in Player.log
8. [D] (only if `det.buzzers` active) def read-back: `RulePackDef` `DV_NamerFactionOutlanderBuzzer` and `DV_NamerSettlementOutlanderBuzzer` no longer contain the rule string `maybeApostrophe->''`
9. [L] confirm every patch file for a mod NOT in the active list produces zero Player.log lines referencing that file's own defNames — logs nothing on no-match, per the anti-guessing rule; this is a negative check and must be run against a KNOWN inactive-mod baseline (e.g. `ab.hoffa`, confirmed absent from the active list per this mod's own loadAfter comment) to be meaningful

## anti-guessing notes
- `AnimalDessicatedTexPaths_Fix.xml` and `GrimTerraTexPaths_Fix.xml` retarget `dessicatedBodyGraphicData`/`bodyGraphicData` `texPath` values on several third-party PawnKindDefs (`BMT_BeardedYak`, `BMT_FreezerFrog`, `Bursa`, `GreaterKraytDragon`, `SW_Electrictick`, `GRimTortoise`, `GRimPinkbird`, `GRimBullfrog`, etc.) — checking these requires the exact NEW texPath value each patch writes, which was not transcribed here; read the patch's own `<value>` node before writing the concrete [D] check, never guess the corrected path.
- `HeadSetForFA_Revive.xml` targets `FacialAnimation.FaceAdjustmentDef` `BS_InsectoidHumanoid_FourArmed_FaceAdjustment` and is a designed no-op today (`ab.hoffa`, the mod it revives, is not in the current active list per the mod's own loadAfter comment) — do not report this patch as "broken" if it produces no visible effect; that is correct until `ab.hoffa` is added.

## [S]
The 60px world-map settlement icon size and its thicker black ring are worth a human glance at the world map, but the numeric check (step 2) already proves the mechanism; no separate human pass is required beyond the mod's own worldview.py render already in use for Ash'karr authoring.
