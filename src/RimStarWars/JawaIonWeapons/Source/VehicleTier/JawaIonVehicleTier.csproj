<Project Sdk="Microsoft.NET.Sdk">

  <!--
    JawaIonVehicleTier - Harmony hook giving the Jawa ion blaster a real effect on
    Vehicle Framework vehicles (VEHICLE_ION_TIER_1). See VehicleIonPatches.cs for
    the full mechanism.

    net48, NOT net472 like the main JawaIonWeapons.csproj. Vehicle Framework's own
    Vehicles.dll and SmashTools.dll are built against .NET Framework 4.8, and
    MSBuild refuses to resolve a reference built against a higher profile than the
    one being targeted - net472 fails the build outright with MSB3274. Same reason
    DesertVehicleReskin/Source/Fuel/DesertVehicleReskin.csproj is net48; copied
    from that known-good project on this machine. There is no Visual Studio or
    targeting pack here, so Microsoft.NETFramework.ReferenceAssemblies supplies
    the reference assemblies from NuGet instead.

    Build (the dotnet is WINDOWS-NATIVE and cannot take a /mnt/d path):
      "%USERPROFILE%\.dotnet\dotnet.exe" build D:\Luke\dev\Rimworld\src\RimStarWars\JawaIonWeapons\Source\VehicleTier\JawaIonVehicleTier.csproj -c Release

    VF_UNGUARDED_EAGER_FIX_1 (2026-09-05): output moved from ../../Assemblies/ to
    ../../VehicleTier/Assemblies/. Sitting in the mod's unconditional Assemblies/
    folder meant this DLL was ALWAYS loaded into the AppDomain regardless of
    Vehicle Framework's presence, so RimWorld's own reflective type scanners
    (the [DebugAction] scanner among them) had to resolve every method signature
    and field initializer in VehicleIonPatches - VehiclePawn/VehicleStatHandler
    included - even with VF absent, throwing TypeLoadException. The
    JawaIonVehicleTierMod AppDomain probe only gated the CALL into
    VehicleIonPatches.Apply(), not the assembly's own load/scan, so it could not
    help. The new location is gated behind
    IfModActive="SmashPhil.VehicleFramework" in ../../LoadFolders.xml (mod
    root): with VF inactive, RimWorld never loads this DLL at all, so no type in
    it is ever resolved. JawaIonWeapons.dll (the main net472 assembly) keeps
    loading from the unconditional ../Assemblies/ - it has no VF type anywhere.

    Every reference is Private=false. Nothing but our own DLL may land next to
    the mod: shipping a copy of Vehicles.dll or 0Harmony.dll would give the game
    two of each and Harmony in particular must come from brrainz.harmony.

    MOD_OPTIONS_RETROFIT_1 (2026-09-11): this project now ProjectReferences the
    sibling net472 JawaIonWeapons.csproj so VehicleIonPatches.cs can read the one
    shared RSW_JawaIonWeaponsSettings (its own master toggle and strength
    multiplier) rather than keep a second, divergent copy of those fields.
    ⚠️ Private=false on that ProjectReference is load-bearing: without it MSBuild
    copies JawaIonWeapons.dll into ../../VehicleTier/Assemblies/ and the mod ships
    TWO copies of the same assembly in two folders the game loads. It is
    compile-time only; the DLL RimWorld loads is the single one in
    ../../Assemblies/, which LoadFolders.xml lists unconditionally and therefore
    always before the VF-gated VehicleTier folder.
    IonDamageDef's own empAmountDroid is STILL read by reflection in
    VehicleIonPatches.cs - that is a per-DamageDef-instance read and unchanged.
  -->

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <LangVersion>latest</LangVersion>
    <AssemblyName>JawaIonVehicleTier</AssemblyName>
    <RootNamespace>JawaIonWeapons</RootNamespace>
    <Version>1.0.0</Version>

    <OutputPath>..\..\VehicleTier\Assemblies\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AssemblySearchPaths>$(AssemblySearchPaths);$(RimWorldManaged)</AssemblySearchPaths>

    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
    <AutoGenerateBindingRedirects>false</AutoGenerateBindingRedirects>
    <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
    <DebugType>none</DebugType>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
  </PropertyGroup>

  <PropertyGroup Condition="'$(RimWorldManaged)' == ''">
    <RimWorldManaged>C:\Program Files (x86)\Steam\steamapps\common\RimWorld\RimWorldWin64_Data\Managed</RimWorldManaged>
  </PropertyGroup>

  <PropertyGroup Condition="'$(HarmonyDll)' == ''">
    <HarmonyDll>C:\Program Files (x86)\Steam\steamapps\workshop\content\294100\2009463077\Current\Assemblies\0Harmony.dll</HarmonyDll>
  </PropertyGroup>

  <!-- Vehicle Framework (SmashPhil). -->
  <PropertyGroup Condition="'$(VehiclesAssemblies)' == ''">
    <VehiclesAssemblies>C:\Program Files (x86)\Steam\steamapps\workshop\content\294100\3014915404\1.6\Assemblies</VehiclesAssemblies>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies" Version="1.0.3" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Assembly-CSharp">
      <HintPath>$(RimWorldManaged)\Assembly-CSharp.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="UnityEngine">
      <HintPath>$(RimWorldManaged)\UnityEngine.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="UnityEngine.CoreModule">
      <HintPath>$(RimWorldManaged)\UnityEngine.CoreModule.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="0Harmony">
      <HintPath>$(HarmonyDll)</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Vehicles">
      <HintPath>$(VehiclesAssemblies)\Vehicles.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="SmashTools">
      <HintPath>$(VehiclesAssemblies)\SmashTools.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <Compile Remove="..\**\*.cs" />
    <Compile Include="*.cs" />
  </ItemGroup>

  <ItemGroup>
    <!-- Compile-time only; see the header. Never copied next to the mod. -->
    <ProjectReference Include="..\JawaIonWeapons.csproj">
      <Private>false</Private>
      <ReferenceOutputAssembly>true</ReferenceOutputAssembly>
    </ProjectReference>
  </ItemGroup>

</Project>
