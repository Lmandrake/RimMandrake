<?xml version="1.0" encoding="utf-8"?>
<ModMetaData>
  <name>RimUtinni: Ash'karr Flora</name>
  <packageId>mandrake.rut.ashkarrflora</packageId>
  <author>Mandrake</author>
  <supportedVersions>
    <li>1.6</li>
  </supportedVersions>
  <description>TREE_GRAPHICS_OWNERSHIP_1: our own tree art at our own scale, per the
owner's ruling — "generate our own tree graphics at the scales we want and drop
all the nonsense of multiple tree mods modifying our designs to who-knows-what
scale." Ships `RUT_SweetlineTree` (`design/Jawa/worldbuilding/biomes/arid_shrubland.md`
§4 "The sweetline, and the gradient that reverses" / "The sweetline trees" —
"huge, ancient, powerful things... never small trees"), a fresh ThingDef with
its own generated art and its own `plant.visualSizeRange`.

Mechanism found and neutralized: three subscribed mods form a data-driven
rescaling framework — `ChaoticEnrico.BetterTrees` (Harmony framework,
`BetterTrees.TreeScaleTemplateDef`/`TreeTextureTemplateDef`, keyed strictly by
`defName`) plus two texture packs that feed it templates,
`Qux.Comigo.BetterTreesMod` and `Maal.BetterTreesMod` ("the whole Comingo tree
thing"). Read from the live Workshop copies (not shippable here — see
`items/TREE_GRAPHICS_OWNERSHIP_1.md`): every template is a `defName -> {texture
dir | minScale/maxScale}` dictionary; there is no category- or
thingClass-wide fallback in the data. `RUT_SweetlineTree` is a brand-new
defName absent from all three mods' template lists, so it is structurally
immune — nothing here needs BetterTrees to be uninstalled. `Patches/BetterTrees_SweetlineTree_Immunity.xml`
adds a defensive `PatchOperationRemove` that strips any future
`RUT_SweetlineTree` entry those three mods' templates might ever add, so an
update to any of them can't silently reclaim our defName. It matches nothing
today — that is by design and is a no-op until proven otherwise (a patch that
matches nothing logs nothing).

Live check still owed (bridge held by another window at authoring time):
spawn `RUT_SweetlineTree`, confirm it renders at its authored
`visualSizeRange` (5.0~6.5, i.e. a canopy roughly 5-6.5 tiles wide at full
growth) and that no BetterTrees console/log line mentions it.

DESERT_STAGGERSEED_BUILD_1 (2026-09-20) — the desert's cycle plant
(`design/Jawa/worldbuilding/biomes/desert.md` §4b "The seed that uses you", §7
"the prepared cycle-seed"). Three defs plus two hediffs, a thought and a recipe:
`RUT_Staggerseed` (the bush, wired into `RUT_Desert`'s wildPlants at 0.1),
`RUT_StaggerseedFruit` (raw, lethal — `RUT_StaggerseedBrood` kills in about
seven hours and there is no tend) and `RUT_StaggerseedSeedDish` (prepared,
euphoric — `RUT_StaggerseedEuphoria`, Cooking 8 to attempt). The
corpse-dispersal half rides
`RimMandrake.CreatureBehaviors.RM_HediffComp_ShadeStagger`: past severity 0.6
the dying are steered toward the best-shaded reachable cell the shade grid
knows of, and the plant germinates at the corpse at a chance that runs from
0.15 in full sun to 1.0 in full shade. The brood sits on the PLANT's own
ingestible as well as on the harvested fruit, because a grazing animal eats the
plant and never the item — without that the whole dispersal cycle would never
fire in a wild biome.

🔴 The plant's SHIPPING NAME is the owner's and is not in yet (§4b: "Name
owner's pick. Working: staggerseed."). Every `RUT_Staggerseed*` defName, label
and texPath here is provisional pending
`infrastructure/state/items/STAGGERSEED_SHIPPING_NAME_1.md`. Art is owed and was
deliberately not queued for the same reason; the graphics point at Core
berry-bush/berry/meal art as a stated interim so nothing renders
magenta.

SWEETLINE_WOOL_HARVEST_1 (2026-09-21) — the harvest arid_shrubland.md §4
promised and never shipped: `RUT_SweetlineTree` inherited TreeBase's
destructive wood harvest, which fells a named landmark tree. Now ships
`RUT_SweetlineWool` (`RUT_SweetlineTree_Items.xml`, ParentName WoolBase,
shaped on `RSW_WoolBantha`) as the tree's harvestedThingDef, with
`harvestAfterGrowth` set (0.05, vanilla's own non-destructive-harvest
mechanism — `PlantProperties.HarvestDestroys` is `harvestAfterGrowth <= 0f`)
so the tree survives every harvest and cut-plant job alike. harvestYield
dropped 160→20: a rare hanging harvest, not lumber. Chose ACCUMULATE over a
flat yield — the wool is *shed* by passing giants, not grown by the tree,
and it is the cheaper build besides (one existing PlantProperties field,
already used twice elsewhere in this mod). Unblocks
`SHRUBLAND_TREE_GUARDIAN_1`. Naming is provisional off "sweetline"/"giant-
wool", never off a giant species name — the giants are still unnamed
(`ARIDSHRUBLAND_SHIPPING_NAMES_1`). Art owed, not queued (Codex quota-blocked
until ~2026-09-26); texPath points at the future render location per this
mod's own established convention.</description>
  <modDependencies>
    <li>
      <packageId>Ludeon.RimWorld</packageId>
      <displayName>RimWorld</displayName>
    </li>
  </modDependencies>
  <loadAfter>
    <li>Ludeon.RimWorld</li>
    <li>ChaoticEnrico.BetterTrees</li>
    <li>Qux.Comigo.BetterTreesMod</li>
    <li>Maal.BetterTreesMod</li>
    <!-- DESERT_STAGGERSEED_BUILD_1: RUT_StaggerseedBrood names
         RimMandrake.CreatureBehaviors.RM_HediffCompProperties_ShadeStagger as a
         comp Class, so that ASSEMBLY must already be loaded when this mod's defs
         are read. modDependencies would assert presence and would NOT order
         anything — the comp entry is MayRequire-guarded instead, so the plant
         degrades to an ordinary lethal fruit with no dispersal if the kit is
         absent, and this loadAfter is what makes it work when it is present. -->
    <li>mandrake.rm.creaturebehaviors</li>
  </loadAfter>
</ModMetaData>
