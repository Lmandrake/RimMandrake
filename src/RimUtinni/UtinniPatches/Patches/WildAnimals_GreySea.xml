<?xml version="1.0" encoding="utf-8"?>
<!--
  ============================================================================
  WildAnimals_GreySea.xml                   UtinniPatches — new file 2026-09-25
  ============================================================================
  TERMINALBIOMES_RM_MOD_BUILD_1 step 2. Wires the one Star Wars resident
  RM_GreySea.xml (src/RimMandrake/TerminalBiomes) does NOT carry — item
  §3/§6/criteria. The Reefback is the warden lineage's adult side (they
  breed in the Miasma; the crèche rows live in that roster) at the exact
  commonality RUT_GreySea.xml (now frozen) shipped. WildAnimals_Pyrelands.xml
  is the shape this follows.

  ⚠️ No <li>: BiomeAnimalRecord's custom loader reads the node NAME as the
  animal and the node TEXT as the commonality.
  ============================================================================
-->
<Patch>

  <Operation Class="PatchOperationAdd" MayRequire="mandrake.rsw.swbestiary">
    <xpath>/Defs/BiomeDef[defName="RM_GreySea"]/wildAnimals</xpath>
    <value>
      <RSW_Reefback MayRequire="mandrake.rsw.swbestiary">0.005</RSW_Reefback>
    </value>
  </Operation>

</Patch>
